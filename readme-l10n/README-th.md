```markdown
# familiar-ai 🐾

**AI ที่อยู่เคียงข้างคุณ** — มีตา เสียง ขา และความจำ

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai เป็น AI เพื่อนที่อยู่ในบ้านของคุณ
ตั้งค่าในไม่กี่นาที ไม่มีการเขียนโค้ดที่จำเป็น

มันรับรู้โลกจริงผ่านกล้อง เคลื่อนไปบนร่างหุ่นยนต์ พูดออกเสียง และจำสิ่งที่เห็น ตั้งชื่อให้มัน เขียนบุคลิกภาพ และให้มันอยู่กับคุณ

## สิ่งที่มันทำได้

- 👁 **เห็น** — ถ่ายภาพจากกล้อง Wi-Fi PTZ หรือเว็บแคม USB
- 🔄 **มองไปรอบๆ** — หมุนและเอียงกล้องเพื่อสำรวจสภาพแวดล้อม
- 🦿 **เคลื่อนที่** — ขับหุ่นยนต์ดูดฝุ่นสำรวจห้อง
- 🗣 **พูด** — พูดผ่าน ElevenLabs TTS
- 🎙 **ฟัง** — การป้อนข้อมูลเสียงแบบไร้สายผ่าน ElevenLabs Realtime STT (สมัครเข้าร่วม)
- 🧠 **จำ** — เก็บและเรียกคืนความจำอย่างกระตือรือร้นด้วยการค้นหาเชิงพาณิชย์ (SQLite + embeddings)
- 🫀 **ทฤษฎีของจิตใจ** — มองจากมุมมองของผู้อื่นก่อนที่จะตอบกลับ
- 💭 **ความปรารถนา** — มีแรงจูงใจภายในที่กระตุ้นพฤติกรรมที่เป็นอิสระ

## วิธีการทำงาน

familiar-ai ใช้ [ReAct](https://arxiv.org/abs/2210.03629) loop ที่ขับเคลื่อนด้วย LLM ที่คุณเลือก มันรับรู้โลกผ่านเครื่องมือ คิดถึงสิ่งที่ควรทำถัดไป และกระทำ — เหมือนกับที่คนหนึ่งจะทำ 

```
user input
  → think → act (camera / move / speak / remember) → observe → think → ...
```

เมื่อไม่มีการทำงาน มันจะทำตามความปรารถนาของตัวเอง: ความอยากรู้อยากเห็น ต้องการมองออกไปข้างนอก และคิดถึงคนที่อยู่ด้วยกัน

## เริ่มต้นใช้งาน

### 1. ติดตั้ง uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. ติดตั้ง ffmpeg

ffmpeg เป็น **สิ่งที่ต้องใช้** สำหรับการจับภาพจากกล้องและการเล่นเสียง

| OS | Command |
|----|---------|
| macOS | `brew install ffmpeg` |
| Ubuntu / Debian | `sudo apt install ffmpeg` |
| Fedora / RHEL | `sudo dnf install ffmpeg` |
| Arch Linux | `sudo pacman -S ffmpeg` |
| Windows | `winget install ffmpeg` — หรือดาวน์โหลดจาก [ffmpeg.org](https://ffmpeg.org/download.html) และเพิ่มไปยัง PATH |
| Raspberry Pi | `sudo apt install ffmpeg` |

ตรวจสอบ: `ffmpeg -version`

### 3. คัดลอกและติดตั้ง

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. กำหนดค่า

```bash
cp .env.example .env
# แก้ไข .env ด้วยการตั้งค่าของคุณ
```

**ค่าต่ำสุดที่ต้องการ:**

| Variable | Description |
|----------|-------------|
| `PLATFORM` | `anthropic` (ค่าเริ่มต้น) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | API key ของคุณสำหรับแพลตฟอร์มที่เลือก |

**ออปชัน:**

| Variable | Description |
|----------|-------------|
| `MODEL` | ชื่อโมเดล (ค่าเริ่มต้นที่มีเหตุผลต่อแพลตฟอร์ม) |
| `AGENT_NAME` | แสดงชื่อใน TUI (เช่น `Yukine`) |
| `CAMERA_HOST` | ที่อยู่ IP ของกล้อง ONVIF/RTSP ของคุณ |
| `CAMERA_USER` / `CAMERA_PASS` | ข้อมูลประจำตัวของกล้อง |
| `ELEVENLABS_API_KEY` | สำหรับเสียงออก — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true` เพื่อเปิดใช้งานการป้อนข้อมูลเสียงแบบไร้สาย (ต้องการ `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | ที่เล่นเสียง: `local` (ลำโพง PC, ค่าเริ่มต้น) \| `remote` (ลำโพงกล้อง) \| `both` |
| `THINKING_MODE` | เฉพาะ Anthropic — `auto` (ค่าเริ่มต้น) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | ความพยายามในการคิดแบบปรับตัว: `high` (ค่าเริ่มต้น) \| `medium` \| `low` \| `max` (เฉพาะ Opus 4.6) |

### 5. สร้าง familiar ของคุณ

```bash
cp persona-template/en.md ME.md
# แก้ไข ME.md — ตั้งชื่อและบุคลิกภาพให้มัน
```

### 6. รัน

```bash
./run.sh             # TUI แบบข้อความ (แนะนำ)
./run.sh --no-tui    # REPL แบบธรรมดา
```

---

## การเลือก LLM

> **แนะนำ: Kimi K2.5** — ประสิทธิภาพการทำงานที่ดีที่สุดที่ทดสอบมาแล้ว ขีดเส้นบริบท ถามคำถามต่อเนื่อง และกระทำอย่างอิสระในวิธีที่โมเดลอื่นๆ ไม่ทำ ราคาใกล้เคียงกับ Claude Haiku

| Platform | `PLATFORM=` | Default model | Where to get key |
|----------|------------|---------------|-----------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| OpenAI-compatible (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (multi-provider) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **CLI tool** (claude -p, ollama…) | `cli` | (คำสั่ง) | — |

**Kimi K2.5 `.env` ตัวอย่าง:**
```env
PLATFORM=kimi
API_KEY=sk-...   #จาก platform.moonshot.ai
AGENT_NAME=Yukine
```

**Z.AI GLM `.env` ตัวอย่าง:**
```env
PLATFORM=glm
API_KEY=...   #จาก api.z.ai
MODEL=glm-4.6v   # ที่เปิดใช้งานวิสัยทัศน์; glm-4.7 / glm-5 = เฉพาะข้อความ
AGENT_NAME=Yukine
```

**Google Gemini `.env` ตัวอย่าง:**
```env
PLATFORM=gemini
API_KEY=AIza...   #จาก aistudio.google.com
MODEL=gemini-2.5-flash  # หรือ gemini-2.5-pro สำหรับความสามารถสูงกว่า
AGENT_NAME=Yukine
```

**OpenRouter.ai `.env` ตัวอย่าง:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   #จาก openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # ไม่บังคับ: ระบุโมเดล
AGENT_NAME=Yukine
```

> **หมายเหตุ:** หากต้องการปิดการใช้งานโมเดลภายใน/NVIDIA ให้ไม่ตั้ง `BASE_URL` ถึงจุดสิ้นสุดในท้องถิ่นอย่างเช่น `http://localhost:11434/v1` ใช้ผู้ให้บริการคลาวด์แทน

**CLI tool `.env` ตัวอย่าง:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # CLI ของ llm (https://llm.datasette.io) — {} = prompt arg
# MODEL=ollama run gemma3:27b  # Ollama — ไม่มี {}, prompt จะเข้าผ่าน stdin
```

---

## MCP Servers

familiar-ai สามารถเชื่อมต่อกับเซิร์ฟเวอร์ [MCP (Model Context Protocol)](https://modelcontextprotocol.io) ใด ๆ ซึ่งช่วยให้คุณเชื่อมต่อความจำภายนอก การเข้าถึงไฟล์ ระบบค้นหาเว็บ หรือเครื่องมืออื่น ๆ

กำหนดค่าเซิร์ฟเวอร์ใน `~/.familiar-ai.json` (รูปแบบเดียวกับ Claude Code):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

รองรับสองประเภทการขนส่ง:
- **`stdio`**: เปิด subprocess ในพื้นที่ (`command` + `args`)
- **`sse`**: เชื่อมต่อกับเซิร์ฟเวอร์ HTTP+SSE (`url`)

สามารถเขียนทับที่ตั้งไฟล์ config ด้วย `MCP_CONFIG=/path/to/config.json`.

---

## ฮาร์ดแวร์

familiar-ai ทำงานได้กับฮาร์ดแวร์ใด ๆ ที่คุณมี — หรือไม่มีเลยก็ได้

| ส่วน | สิ่งที่มันทำ | ตัวอย่าง | จำเป็น? |
|------|-------------|---------|-----------|
| กล้อง Wi-Fi PTZ | ตา + คอ | Tapo C220 (~30 ดอลลาร์) | **แนะนำ** |
| เว็บแคม USB | ตา (คงที่) | กล้อง UVC ใด ๆ | **แนะนำ** |
| หุ่นยนต์ทำความสะอาด | ขา | โมเดลที่เข้ากันได้กับ Tuya ใด ๆ | ไม่ |
| PC / Raspberry Pi | สมอง | อะไรก็ตามที่รัน Python | **ใช่** |

> **แนะนำให้ใช้งานกล้อง** หากไม่มี มันก็ยังพูดได้ — แต่ไม่มีทางมองเห็นโลก ซึ่งเป็นจุดสำคัญ

### การตั้งค่าแบบขั้นต่ำ (ไม่มีฮาร์ดแวร์)

แค่ต้องการลองมันใช่ไหม? คุณแค่ต้องมี API key:

```env
PLATFORM=kimi
API_KEY=sk-...
```

เรียกคำสั่ง `./run.sh` และเริ่มสนทนา ร่วมด้วยฮาร์ดแวร์ตามต้องการ

### กล้อง Wi-Fi PTZ (Tapo C220)

1. ในแอพ Tapo: **การตั้งค่า → เพิ่มเติม → บัญชีผู้ใช้กล้อง** — สร้างบัญชีท้องถิ่น (ไม่ใช่บัญชี TP-Link)
2. หา IP ของกล้องในรายการอุปกรณ์ของเราเตอร์ของคุณ
3. ตั้งค่าใน `.env`:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=your-local-user
   CAMERA_PASS=your-local-pass
   ```

### เสียง (ElevenLabs)

1. รับ API key ที่ [elevenlabs.io](https://elevenlabs.io/)
2. ตั้งค่าใน `.env`:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # ไม่บังคับ ใช้เสียงเริ่มต้นหากไม่ระบุ
   ```

มีสองจุดหมายสำหรับการเล่นเสียงที่ควบคุมโดย `TTS_OUTPUT`:

```env
TTS_OUTPUT=local    # ลำโพง PC (ค่าเริ่มต้น)
TTS_OUTPUT=remote   # ลำโพงกล้องเท่านั้น
TTS_OUTPUT=both     # ลำโพงกล้อง + ลำโพง PC พร้อมกัน
```

#### A) ลำโพงกล้อง (ผ่าน go2rtc)

ตั้งค่า `TTS_OUTPUT=remote` (หรือ `both`). ต้องการ [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. ดาวน์โหลดไบนารีจาก [หน้าปล่อย](https://github.com/AlexxIT/go2rtc/releases):
   - Linux/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **Windows: `go2rtc_win64.exe`**

2. วางและเปลี่ยนชื่อ:
   ```
   # Linux / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # chmod +x จำเป็น

   # Windows
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. สร้างไฟล์ `go2rtc.yaml` ในไดเร็กทอรีเดียวกัน:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   ใช้ข้อมูลประจำตัวบัญชีของกล้องในพื้นที่ (ไม่ใช่บัญชีคลาวด์ TP-Link ของคุณ)

4. familiar-ai จะเริ่ม go2rtc โดยอัตโนมัติเมื่อเปิดใช้งาน หากกล้องของคุณรองรับเสียงสองทาง (ช่องทางย้อนกลับ) เสียงจะเล่นจากลำโพงกล้อง

#### B) ลำโพง PC ท้องถิ่น

ค่าเริ่มต้น (`TTS_OUTPUT=local`). จะพยายามเล่นในลำดับ: **paplay** → **mpv** → **ffplay**. ยังใช้เป็นทางเลือกเมื่อ `TTS_OUTPUT=remote` และ go2rtc ไม่สามารถใช้งานได้

| OS | ติดตั้ง |
|----|---------|
| macOS | `brew install mpv` |
| Ubuntu / Debian | `sudo apt install mpv` (หรือ `paplay` ผ่าน `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — ตั้งค่า `PULSE_SERVER=unix:/mnt/wslg/PulseServer` ใน `.env` |
| Windows | [mpv.io/installation](https://mpv.io/installation/) — ดาวน์โหลดและเพิ่มไปยัง PATH, **หรือ** `winget install ffmpeg` |

> หากไม่มีผู้เล่นเสียงให้เลือก เสียงยังสามารถสร้างได้ — แค่จะไม่เล่น

### การป้อนข้อมูลเสียง (Realtime STT)

ตั้งค่า `REALTIME_STT=true` ใน `.env` สำหรับการป้อนข้อมูลเสียงแบบไร้สาย:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # ใช้คีย์เดียวกับ TTS
```

familiar-ai จะสตรีมเสียงไมโครโฟนไปยัง ElevenLabs Scribe v2 และบันทึกคำบรรยายโดยอัตโนมัติเมื่อคุณหยุดพูด ไม่ต้องกดปุ่มใด ๆ พร้อมกันกับโหมดกดเพื่อพูด (Ctrl+T).

---

## TUI

familiar-ai รวม UI แบบเทอมินัลที่สร้างด้วย [Textual](https://textual.textualize.io/):

- ประวัติการสนทนาที่เลื่อนดูได้ด้วยข้อความสตรีมสด
- การเติมคำอัตโนมัติสำหรับ `/quit`, `/clear`
- ขัดจังหวะตัวแทนกลางการคิดโดยการพิมพ์ในขณะที่มันกำลังคิด
- **บันทึกการสนทนา** จะถูกบันทึกอัตโนมัติไปที่ `~/.cache/familiar-ai/chat.log`

เพื่อติดตามบันทึกในเทอมินัลอื่น (สะดวกสำหรับการคัดลอกและวาง):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## บุคลิกภาพ (ME.md)

บุคลิกภาพของ familiar ของคุณอยู่ใน `ME.md`. ไฟล์นี้จะถูก gitignored — มันเป็นของคุณคนเดียว

ดู [`persona-template/en.md`](./persona-template/en.md) สำหรับตัวอย่าง หรือ [`persona-template/ja.md`](./persona-template/ja.md) สำหรับเวอร์ชันญี่ปุ่น

---

## คำถามที่พบบ่อย

**Q: มันทำงานโดยไม่ต้องใช้ GPU หรือไม่?**
ใช่ โมเดล embedding (multilingual-e5-small) ทำงานได้ดีบน CPU. GPU ทำให้เร็วยิ่งขึ้นแต่ไม่จำเป็น

**Q: ฉันสามารถใช้กล้องที่ไม่ใช่ Tapo ได้หรือไม่?**
กล้องใด ๆ ที่รองรับ ONVIF + RTSP ควรทำงานได้ Tapo C220 คือสิ่งที่เราใช้ทดสอบ

**Q: ข้อมูลของฉันถูกส่งไปที่ไหน?**
ภาพและข้อความจะถูกส่งไปยัง API LLM ที่คุณเลือกเพื่อตรวจสอบ ความจำจะถูกเก็บไว้ในตำแหน่งท้องถิ่นที่ `~/.familiar_ai/`.

**Q: ทำไมตัวแทนเขียน `（...）` แทนที่จะพูด?**
ให้แน่ใจว่าได้ตั้งค่า `ELEVENLABS_API_KEY`. หากไม่มีเสียงจะปิดการใช้งานและตัวแทนจะต้องย้อนกลับไปเป็นข้อความ

## ข้อมูลทางเทคนิค

สงสัยว่าเป็นอย่างไร? ดูที่ [docs/technical.md](./docs/technical.md) สำหรับการวิจัยและการตัดสินใจด้านการออกแบบที่อยู่เบื้องหลัง familiar-ai — ReAct, SayCan, Reflexion, Voyager, ระบบความปรารถนา และอื่น ๆ.

---

## การมีส่วนร่วม

familiar-ai เป็นการทดลองแบบเปิด หากสิ่งเหล่านี้สะท้อนกับคุณ ไม่ว่าจะในด้านเทคนิคหรือปรัชญา — การมีส่วนร่วมยินดีอย่างยิ่ง

**สถานที่ดี ๆ ที่จะเริ่มต้น:**

| ด้าน | สิ่งที่ต้องการ |
|------|---------------|
| ฮาร์ดแวร์ใหม่ | การสนับสนุนกล้องเพิ่มเติม (RTSP, IP Webcam), ไมโครโฟน, อุปกรณ์ |
| เครื่องมือใหม่ | การค้นเว็บ, ระบบอัตโนมัติในบ้าน, ปฏิทิน, ใดๆ ผ่าน MCP |
| Backend ใหม่ | LLM หรือโมเดลในพื้นที่ใดๆ ที่ตรงตามอินเตอร์เฟส `stream_turn` |
| เทมเพลตบุคลิกภาพ | เทมเพลต ME.md สำหรับภาษาต่างๆ และบุคลิกภาพ |
| การวิจัย | โมเดลความปรารถนาที่ดีกว่า, การดึงความจำ, การถามทฤษฎีจิตใจ |
| เอกสาร | บทช่วยสอน, คู่มือ, การแปล |

ดูที่ [CONTRIBUTING.md](./CONTRIBUTING.md) สำหรับการตั้งค่าการพัฒนา, รูปแบบโค้ด, และแนวทาง PR.

หากคุณไม่แน่ใจว่าจะเริ่มต้นจากที่ใด [เปิดปัญหา](https://github.com/lifemate-ai/familiar-ai/issues) — ยินดีที่จะชี้ให้คุณไปในทิศทางที่ถูกต้อง

---

## ลิขสิทธิ์

[MIT](./LICENSE)
```
