# familiar-ai 🐾

**Штучний інтелект, що живе поруч з вами** — з очима, голосом, ногами та пам'яттю.

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai — це супутник ШІ, який живе у вашому домі. Налаштуйте його за кілька хвилин. Кодування не потрібно.

Він сприймає реальний світ через камери, рухається на роботизованому тілі, говорить вголос і запам'ятовує те, що бачить. Дайте йому ім'я, напишіть його особистість і дозвольте йому жити з вами.

## Що він може робити

- 👁 **Бачити** — захоплює зображення з PTZ-камери Wi-Fi або USB-камери
- 🔄 **Досліджувати** — обертає та нахиляє камеру, щоб дослідити навколишнє середовище
- 🦿 **Рухатися** — пересуває робот-пилосос по кімнаті
- 🗣 **Говорити** — говорить за допомогою TTS ElevenLabs
- 🎙 **Слухати** — безконтактний голосовий ввід з використанням Realtime STT ElevenLabs (за бажанням)
- 🧠 **Запам'ятовувати** — активно зберігає та відтворює спогади за допомогою семантичного пошуку (SQLite + ембедінги)
- 🫀 **Теорія розуму** — приймає перспективу іншої особи перед відповіддю
- 💭 **Бажання** — має свої внутрішні імпульси, які викликають автономну поведінку

## Як це працює

familiar-ai запускає цикл [ReAct](https://arxiv.org/abs/2210.03629), підживлюваний вашим вибором LLM. Він сприймає світ через інструменти, обмірковує, що робити далі, та діє — так, як це робила б людина.

```
user input
  → think → act (camera / move / speak / remember) → observe → think → ...
```

Коли неактивний, він діє на свої власні бажання: цікавість, бажання подивитися на вулицю, ностальгія за людиною, з якою живе.

## Початок роботи

### 1. Встановіть uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. Встановіть ffmpeg

ffmpeg є **обов'язковим** для захоплення зображень з камери та відтворення аудіо.

| ОС | Команда |
|----|---------|
| macOS | `brew install ffmpeg` |
| Ubuntu / Debian | `sudo apt install ffmpeg` |
| Fedora / RHEL | `sudo dnf install ffmpeg` |
| Arch Linux | `sudo pacman -S ffmpeg` |
| Windows | `winget install ffmpeg` — або завантажте з [ffmpeg.org](https://ffmpeg.org/download.html) та додайте в PATH |
| Raspberry Pi | `sudo apt install ffmpeg` |

Перевірте: `ffmpeg -version`

### 3. Клонуйте та встановіть

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. Налаштуйте

```bash
cp .env.example .env
# Відредагуйте .env зі своїми налаштуваннями
```

**Мінімально необхідні:**

| Змінна | Опис |
|----------|-------------|
| `PLATFORM` | `anthropic` (за замовчуванням) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | Ваш ключ API для вибраної платформи |

**Необов'язкові:**

| Змінна | Опис |
|----------|-------------|
| `MODEL` | Назва моделі (налаштовані значення за замовчуванням для кожної платформи) |
| `AGENT_NAME` | Відображуване ім'я, яке показується в TUI (наприклад, `Yukine`) |
| `CAMERA_HOST` | IP-адреса вашої камери ONVIF/RTSP |
| `CAMERA_USER` / `CAMERA_PASS` | Облікові дані камери |
| `ELEVENLABS_API_KEY` | Для виходу голосу — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true`, щоб увімкнути завжди активний безконтактний голосовий ввід (вимагає `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | Де відтворювати аудіо: `local` (колонки ПК, за замовчуванням) \| `remote` (динамік камери) \| `both` |
| `THINKING_MODE` | Лише для Anthropic — `auto` (за замовчуванням) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | Адаптивні зусилля мислення: `high` (за замовчуванням) \| `medium` \| `low` \| `max` (лише Opus 4.6) |

### 5. Створіть свого знайомого

```bash
cp persona-template/en.md ME.md
# Відредагуйте ME.md — дайте йому ім'я та особистість
```

### 6. Запустіть

```bash
./run.sh             # Текстовий TUI (рекомендується)
./run.sh --no-tui    # Звичайний REPL
```

---

## Вибір LLM

> **Рекомендується: Kimi K2.5** — найкраща агентна продуктивність з тих, що тестувалися. Звертає увагу на контекст, ставить додаткові запитання і діє автономно в способах, яких не роблять інші моделі. Ціна схожа на Claude Haiku.

| Платформа | `PLATFORM=` | Модель за замовчуванням | Де отримати ключ |
|----------|------------|---------------|-----------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| OpenAI-сумісні (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (багатопровайдерні) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **CLI tool** (claude -p, ollama…) | `cli` | (команда) | — |

**Приклад `.env` для Kimi K2.5:**
```env
PLATFORM=kimi
API_KEY=sk-...   # з platform.moonshot.ai
AGENT_NAME=Yukine
```

**Приклад `.env` для Z.AI GLM:**
```env
PLATFORM=glm
API_KEY=...   # з api.z.ai
MODEL=glm-4.6v   # зображення дозволено; glm-4.7 / glm-5 = лише текст
AGENT_NAME=Yukine
```

**Приклад `.env` для Google Gemini:**
```env
PLATFORM=gemini
API_KEY=AIza...   # з aistudio.google.com
MODEL=gemini-2.5-flash  # або gemini-2.5-pro для більшої здатності
AGENT_NAME=Yukine
```

**Приклад `.env` для OpenRouter.ai:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   # з openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # за бажанням: вказати модель
AGENT_NAME=Yukine
```

> **Примітка:** Щоб вимкнути локальні/NVIDIA моделі, просто не встановлюйте `BASE_URL` на локальний кінцевий пункт, наприклад `http://localhost:11434/v1`. Використовуйте хмарних провайдерів замість цього.

**Приклад `.env` для CLI tool:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # llm CLI (https://llm.datasette.io) — {} = аргумент промпта
# MODEL=ollama run gemma3:27b  # Ollama — без {}, промпт через stdin
```

---

## MCP-сервери

familiar-ai може підключатися до будь-якого [MCP (Model Context Protocol)](https://modelcontextprotocol.io) сервера. Це дозволяє вам підключити зовнішню пам'ять, доступ до файлової системи, веб-пошук або будь-який інший інструмент.

Налаштуйте сервери в `~/.familiar-ai.json` (те ж формати, що і Claude Code):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

Підтримуються два типи транспорту:
- **`stdio`**: запускає локальний підпроцес (`command` + `args`)
- **`sse`**: підключається до сервера HTTP+SSE (`url`)

Перезаписати місце розташування конфігураційного файлу можна за допомогою `MCP_CONFIG=/path/to/config.json`.

---

## Апаратура

familiar-ai працює з будь-яким обладнанням, яке у вас є — або зовсім без нього.

| Частина | Що робить | Приклад | Обов'язково? |
|------|-------------|---------|-----------|
| Wi-Fi PTZ камера | Очі + шия | Tapo C220 (~$30) | **Рекомендується** |
| USB-камера | Очі (фіксовані) | Будь-яка UVC-камера | **Рекомендується** |
| Робот-пилосос | Ноги | Будь-яка модель, сумісна з Tuya | Ні |
| ПК / Raspberry Pi | Мозок | Будь-що, що працює на Python | **Так** |

> **Камера настійно рекомендується.** Без неї familiar-ai може все ще говорити — але не може бачити світ, що, по суті, є всім сенсом.

### Мінімальне налаштування (без апаратури)

Хочете тільки спробувати? Вам потрібен лише ключ API:

```env
PLATFORM=kimi
API_KEY=sk-...
```

Запустіть `./run.sh` і почніть спілкування. Додавайте апаратуру за необхідності.

### Wi-Fi PTZ камера (Tapo C220)

1. У додатку Tapo: **Налаштування → Розширені налаштування → Обліковий запис камери** — створіть локальний обліковий запис (не TP-Link)
2. Знайдіть IP-адресу камери в списку пристроїв вашого роутера
3. Встановіть в `.env`:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=your-local-user
   CAMERA_PASS=your-local-pass
   ```

### Голос (ElevenLabs)

1. Отримайте ключ API на [elevenlabs.io](https://elevenlabs.io/)
2. Встановіть в `.env`:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # необов'язково, використовується стандартний голос якщо опущено
   ```

Існують дві дестинації відтворення, контрольовані `TTS_OUTPUT`:

```env
TTS_OUTPUT=local    # колонки ПК (за замовчуванням)
TTS_OUTPUT=remote   # лише динамік камери
TTS_OUTPUT=both     # динамік камери + колонки ПК одночасно
```

#### A) Динамік камери (через go2rtc)

Встановіть `TTS_OUTPUT=remote` (або `both`). Вимагає [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. Завантажте двійковий файл з [сторінки релізів](https://github.com/AlexxIT/go2rtc/releases):
   - Linux/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **Windows: `go2rtc_win64.exe`**

2. Помістіть та перейменуйте його:
   ```
   # Linux / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # chmod +x необхідно

   # Windows
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. Створіть `go2rtc.yaml` у тій же директорії:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   Використовуйте облікові дані локального облікового запису камери (не ваш TP-Link Cloud обліковий запис).

4. familiar-ai автоматично запускає go2rtc під час запуску. Якщо ваша камера підтримує двосторонній звук (зворотний канал), голос звучить з динаміка камери.

#### B) Локальні колонки ПК

За замовчуванням (`TTS_OUTPUT=local`). Намагається відтворювати через програвачі в такому порядку: **paplay** → **mpv** → **ffplay**. Також використовується як резерв, коли `TTS_OUTPUT=remote` і go2rtc недоступний.

| ОС | Встановлення |
|----|---------|
| macOS | `brew install mpv` |
| Ubuntu / Debian | `sudo apt install mpv` (або `paplay` через `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — встановіть `PULSE_SERVER=unix:/mnt/wslg/PulseServer` в `.env` |
| Windows | [mpv.io/installation](https://mpv.io/installation/) — завантажте та додайте в PATH, **або** `winget install ffmpeg` |

> Якщо жоден аудіопрогравач не доступний, мова все ще генерується — просто не буде відтворюватися.

### Голосовий ввід (Realtime STT)

Встановіть `REALTIME_STT=true` в `.env` для завжди активного безконтактного голосового вводу:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # той самий ключ, що й TTS
```

familiar-ai транслює аудіо з мікрофона в ElevenLabs Scribe v2 і автоматично фіксує транскрипції, коли ви призупиняєте мовлення. Додаткові натискання не потрібні. Співіснує з режимом натисни-щоб-поговорити (Ctrl+T).

---

## TUI

familiar-ai включає термінальний інтерфейс, створений з [Textual](https://textual.textualize.io/):

- Прокручувана історія розмови з живим потоком тексту
- Автозавершення для `/quit`, `/clear`
- Перервати агента в середині думки, набираючи, поки він обмірковує
- **Журнал розмови** автоматично зберігається в `~/.cache/familiar-ai/chat.log`

Щоб слідкувати за журналом в іншому терміналі (зручно для копіювання-вставки):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## Особистість (ME.md)

Особистість вашого знайомого зберігається в `ME.md`. Цей файл ігнорується git — він лише ваш.

Дивіться [`persona-template/en.md`](./persona-template/en.md) для прикладу або [`persona-template/ja.md`](./persona-template/ja.md) для японської версії.

---

## ЧаПи

**Q: Чи працює без GPU?**
Так. Модель вбудовування (multilingual-e5-small) добре працює на CPU. GPU робить це швидше, але не є обов'язковим.

**Q: Чи можу я використовувати камеру, іншу, ніж Tapo?**
Будь-яка камера, яка підтримує ONVIF + RTSP, повинна працювати. Tapo C220 — це те, з чим ми тестували.

**Q: Чи надсилаються мої дані кудись?**
Зображення та текст надсилаються в обрану вами LLM API для обробки. Спогади зберігаються локально в `~/.familiar_ai/`.

**Q: Чому агент пише `（...）`, а не говорить?**
Переконайтеся, що `ELEVENLABS_API_KEY` налаштований. Без нього голос вимкнений, і агент повертається до тексту.

## Технічний фон

Цікаво, як це працює? Дивіться [docs/technical.md](./docs/technical.md) для досліджень і дизайнерських рішень, які стоять за familiar-ai — ReAct, SayCan, Reflexion, Voyager, система бажань та інше.

---

## Участь

familiar-ai — це відкрите експеримент. Якщо вам щось із цього резонує — технічно або філософськи — внески дуже вітаються.

**Хороші місця для початку:**

| Область | Що потрібно |
|------|---------------|
| Нова апаратура | Підтримка більше камер (RTSP, IP Webcam), мікрофонів, актуаторів |
| Нові інструменти | Веб-пошук, домашня автоматизація, календар, будь-що через MCP |
| Нові бекенди | Будь-яка LLM або локальна модель, яка підходить під інтерфейс `stream_turn` |
| Шаблони персон | Шаблони ME.md для різних мов і особистостей |
| Дослідження | Кращі моделі бажань, витягування пам'яті, запити на теорію розуму |
| Документація | Посібники, проходження, переклади |

Дивіться [CONTRIBUTING.md](./CONTRIBUTING.md) для налаштувань розробки, стилю коду та вказівок щодо PR.

Якщо ви не знаєте, з чого почати, [відкрийте проблему](https://github.com/lifemate-ai/familiar-ai/issues) — з радістю вкажемо вам правильний напрямок.

---

## Ліцензія

[MIT](./LICENSE)
