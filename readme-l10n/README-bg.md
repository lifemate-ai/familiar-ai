```markdown
# familiar-ai 🐾

**Изкуствен интелект, който живее до теб** — с очи, глас, крака и памет.

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai е спътник на изкуствения интелект, който живее в твоя дом. Настрой го за минути. Не се изисква програмиране.

Той възприема реалния свят чрез камери, движи се на роботско тяло, говори на глас и запомня, какво вижда. Дай му име, напиши неговия характер и го остави да живее с теб.

## Какво може да прави

- 👁 **Виж** — улавя изображения от Wi-Fi PTZ камера или USB уеб камера
- 🔄 **Огледай се** — панорамира и накланя камерата, за да изследва околността
- 🦿 **Движи се** — управлява роботизирана прахосмукачка, за да се разхожда в стаята
- 🗣 **Говори** — говори чрез ElevenLabs TTS
- 🎙 **Слушай** — без ръце гласов вход чрез ElevenLabs Realtime STT (по желание)
- 🧠 **Запомни** — активно съхранява и припомня спомени с семантично търсене (SQLite + embeddings)
- 🫀 **Теория на ума** — взема предвид перспективата на другия човек преди да отговори
- 💭 **Желание** — има свои вътрешни стремежи, които предизвикват автономно поведение

## Как работи

familiar-ai работи с [ReAct](https://arxiv.org/abs/2210.03629) цикъл, захранван от твоя избор на LLM. Той възприема света чрез инструменти, мисли какво да направи следващо и действа — точно както би направил човек.

```
user input
  → think → act (camera / move / speak / remember) → observe → think → ...
```

Когато е в покой, той действа според собствените си желания: любопитство, желание да погледне навън, липсата на човека, с когото живее.

## Започване

### 1. Инсталирай uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. Инсталирай ffmpeg

ffmpeg е **необходим** за улавяне на изображения от камера и възпроизвеждане на аудио.

| OS | Команда |
|----|---------|
| macOS | `brew install ffmpeg` |
| Ubuntu / Debian | `sudo apt install ffmpeg` |
| Fedora / RHEL | `sudo dnf install ffmpeg` |
| Arch Linux | `sudo pacman -S ffmpeg` |
| Windows | `winget install ffmpeg` — или свали от [ffmpeg.org](https://ffmpeg.org/download.html) и добави в PATH |
| Raspberry Pi | `sudo apt install ffmpeg` |

Провери: `ffmpeg -version`

### 3. Клонирай и инсталирай

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. Конфигурирай

```bash
cp .env.example .env
# Редактирай .env с твоите настройки
```

**Минимално изискващо:**

| Променлива | Описание |
|------------|----------|
| `PLATFORM` | `anthropic` (по подразбиране) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | Твоят API ключ за избраната платформа |

**Опционално:**

| Променлива | Описание |
|------------|----------|
| `MODEL` | Име на модела (умерени подразбиращи се настройки за всяка платформа) |
| `AGENT_NAME` | Показвано име в TUI (напр. `Yukine`) |
| `CAMERA_HOST` | IP адрес на твоята ONVIF/RTSP камера |
| `CAMERA_USER` / `CAMERA_PASS` | Удостоверения за камерата |
| `ELEVENLABS_API_KEY` | За гласово изход — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true` за активиране на винаги включен гласов вход (изисква `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | Къде да се възпроизвежда аудиото: `local` (високоговорител на компютъра, по подразбиране) \| `remote` (високоговорител на камерата) \| `both` |
| `THINKING_MODE` | Само Anthropic — `auto` (по подразбиране) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | Адаптивно усилие при мислене: `high` (по подразбиране) \| `medium` \| `low` \| `max` (само за Opus 4.6) |

### 5. Създай твоето familiar

```bash
cp persona-template/en.md ME.md
# Редактирай ME.md — дай му име и характер
```

### 6. Стартирай

```bash
./run.sh             # Текстов TUI (препоръчително)
./run.sh --no-tui    # Обикновен REPL
```

---

## Избор на LLM

> **Препоръчително: Kimi K2.5** — най-добро агентно представяне, тествано до момента. Забелязва контекста, задава последващи въпроси и действа автономно по начини, по които другите модели не го правят. Ценовото равнище е подобно на това на Claude Haiku.

| Платформа | `PLATFORM=` | Подразбиращ се модел | Къде да получиш ключ |
|-----------|-------------|----------------------|---------------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| OpenAI-съвместими (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (много-доставчик) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **CLI инструмент** (claude -p, ollama…) | `cli` | (командата) | — |

**Пример за `.env` на Kimi K2.5:**
```env
PLATFORM=kimi
API_KEY=sk-...   # от platform.moonshot.ai
AGENT_NAME=Yukine
```

**Пример за `.env` на Z.AI GLM:**
```env
PLATFORM=glm
API_KEY=...   # от api.z.ai
MODEL=glm-4.6v   # с активирана визия; glm-4.7 / glm-5 = само текст
AGENT_NAME=Yukine
```

**Пример за `.env` на Google Gemini:**
```env
PLATFORM=gemini
API_KEY=AIza...   # от aistudio.google.com
MODEL=gemini-2.5-flash  # или gemini-2.5-pro за по-висока способност
AGENT_NAME=Yukine
```

**Пример за `.env` на OpenRouter.ai:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   # от openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # опционално: уточни модел
AGENT_NAME=Yukine
```

> **Забележка:** За да деактивираш локалните/NVIDIA модели, просто не задавай `BASE_URL` на локален крайна точка като `http://localhost:11434/v1`. Използвай облачни доставчици вместо това.

**Пример за `.env` на CLI инструмент:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # llm CLI (https://llm.datasette.io) — {} = аргумент на подканата
# MODEL=ollama run gemma3:27b  # Ollama — без {}, подканата минава през stdin
```

---

## MCP Сървъри

familiar-ai може да се свърже с всеки [MCP (Model Context Protocol)](https://modelcontextprotocol.io) сървър. Това ти позволява да вкараш външна памет, достъп до файловата система, уеб търсене или какъвто и да е друг инструмент.

Конфигурирай сървъри в `~/.familiar-ai.json` (същия формат като Claude Code):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

Поддържат се два типа транспорт:
- **`stdio`**: стартиране на локален подпроцес (`command` + `args`)
- **`sse`**: свързване с HTTP+SSE сървър (`url`)

Овъррайдни конфигурационни файлове с `MCP_CONFIG=/path/to/config.json`.

---

## Хардуер

familiar-ai работи с какъвто и да е хардуер, който имаш — или без никакъв.

| Час | Какво прави | Пример | Изисква се? |
|------|-------------|---------|-----------|
| Wi-Fi PTZ камера | Очите + шия | Tapo C220 (~$30) | **Препоръчително** |
| USB уеб камера | Очи (фиксирани) | Всяка UVC камера | **Препоръчително** |
| Роботизирана прахосмукачка | Крака | Всяки модели, съвместими с Tuya | Не |
| PC / Raspberry Pi | Мозък | Нещо, което работи с Python | **Да** |

> **Камера е силно препоръчителна.** Без камера familiar-ai все още може да говори — но не може да вижда света, което е съвсем основната идея.

### Минимална настройка (без хардуер)

Искаш само да опиташ? Нужен ти е само API ключ:

```env
PLATFORM=kimi
API_KEY=sk-...
```

Стартирай `./run.sh` и започни да чатиш. Добавяй хардуер по пътя.

### Wi-Fi PTZ камера (Tapo C220)

1. В приложението Tapo: **Настройки → Разширени → Камерна сметка** — създай локална сметка (не TP-Link сметка)
2. Намери IP адреса на камерата в списъка с устройства на рутера си
3. Задай в `.env`:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=твоят-локален-потребител
   CAMERA_PASS=твоят-локален-парола
   ```

### Глас (ElevenLabs)

1. Вземи API ключ на [elevenlabs.io](https://elevenlabs.io/)
2. Задай в `.env`:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # опционално, използва подразбиращия се глас, ако не е зададен
   ```

Има две дестинации за възпроизвеждане, контролирани от `TTS_OUTPUT`:

```env
TTS_OUTPUT=local    # високоговорител на компютъра (по подразбиране)
TTS_OUTPUT=remote   # само високоговорител на камерата
TTS_OUTPUT=both     # високоговорител на камерата + високоговорител на компютъра едновременно
```

#### А) Високоговорител на камерата (чрез go2rtc)

Задай `TTS_OUTPUT=remote` (или `both`). Изисква [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. Изтегли бинарния файл от [страницата с релизации](https://github.com/AlexxIT/go2rtc/releases):
   - Linux/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **Windows: `go2rtc_win64.exe`**

2. Постави и преименувай:
   ```
   # Linux / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # chmod +x е необходимо

   # Windows
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. Създай `go2rtc.yaml` в същата директория:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   Използвай удостоверенията на локалната камера (не твоят TP-Link облачен акаунт).

4. familiar-ai стартира go2rtc автоматично при стартиране. Ако камерата ти поддържа двустранно аудио (обратен канал), гласът ще се възпроизвежда от високоговорителя на камерата.

#### Б) Локален високоговорител на компютъра

По подразбиране (`TTS_OUTPUT=local`). Опитва плеъри по ред: **paplay** → **mpv** → **ffplay**. Използва се и като резервно решение, когато `TTS_OUTPUT=remote` и go2rtc не е наличен.

| OS | Инсталирай |
|----|------------|
| macOS | `brew install mpv` |
| Ubuntu / Debian | `sudo apt install mpv` (или `paplay` чрез `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — задайте `PULSE_SERVER=unix:/mnt/wslg/PulseServer` в `.env` |
| Windows | [mpv.io/installation](https://mpv.io/installation/) — свали и добави в PATH, **или** `winget install ffmpeg` |

> Ако не е наличен аудиоплеър, речта все още се генерира — просто няма да се възпроизвежда.

### Гласов вход (Realtime STT)

Задай `REALTIME_STT=true` в `.env` за винаги включен, без ръце гласов вход:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # същият ключ като TTS
```

familiar-ai предава аудио от микрофона на ElevenLabs Scribe v2 и автоматично записва транскрипцията, когато спреш да говориш. Не е необходима натиска на бутон. Съществува паралелно с режима за натискане за разговор (Ctrl+T).

---

## TUI

familiar-ai включва терминален UI, изграден с [Textual](https://textual.textualize.io/):

- Превъртаща се история на разговора с текст, който се предава на живо
- Завършване на табула за `/quit`, `/clear`
- Прекъснете агента по време на размисъл, като пишете, докато мисли
- **История на разговора** автоматично запазена в `~/.cache/familiar-ai/chat.log`

За да следите логовете в друг терминал (полезно за копиране-залепване):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## Лична (ME.md)

Личността на твоето familiar живее в `ME.md`. Тази файл е gitignored — само ти я притежаваш.

Виж [`persona-template/en.md`](./persona-template/en.md) за пример, или [`persona-template/ja.md`](./persona-template/ja.md) за японска версия.

---

## Често задавани въпроси

**В: Работи ли без GPU?**
Да. Моделът с вграждане (multilingual-e5-small) работи отлично на CPU. GPU прави работата по-бърза, но не е необходим.

**В: Мога ли да използвам камера, различна от Tapo?**
Всяка камера, която поддържа ONVIF + RTSP, трябва да работи. Tapo C220 е тестовата камера, която използвахме.

**В: Изпращат ли се данните ми някъде?**
Снимки и текст се изпращат до избраната LLM API за обработка. Спомените се съхраняват локално в `~/.familiar_ai/`.

**В: Защо агентът пише `（...）` вместо да говори?**
Увери се, че `ELEVENLABS_API_KEY` е зададен. Без него, гласът е деактивиран и агентът се връща към текст.

## Технически фон

Любопитен как работи? Виж [docs/technical.md](./docs/technical.md) за изследванията и проектните решения зад familiar-ai — ReAct, SayCan, Reflexion, Voyager, системата на желанията и много други.

---

## Приноси

familiar-ai е отворен експеримент. Ако нещо от това резонира с теб — технически или философски — приносите са много добре дошли.

**Добри места за начало:**

| Област | Какво е необходимо |
|--------|------------------|
| Нов хардуер | Подкрепа за повече камери (RTSP, IP Webcam), микрофони, активатори |
| Нови инструменти | Уеб търсене, автоматизация на дома, календар, всичко чрез MCP |
| Нови бекенди | Всеки LLM или локален модел, който отговаря на интерфейса `stream_turn` |
| Шаблони за личност | ME.md шаблони за различни езици и личности |
| Изследвания | По-добри модели на желания, извличане на памет, подканяне на теория на ума |
| Документация | Уроки, walkthroughs, преводи |

Виж [CONTRIBUTING.md](./CONTRIBUTING.md) за настройка на разработка, стил на кодиране и напътствия за PR.

Ако не знаеш откъде да започнеш, [отворете проблем](https://github.com/lifemate-ai/familiar-ai/issues) — с радост ще те насочим в правилната посока.

---

## Лиценз

[MIT](./LICENSE)
```
