```markdown
# familiar-ai 🐾

**یک هوش مصنوعی که در کنار شما زندگی می‌کند** — با چشم‌ها، صدا، پاها و حافظه.

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai یک همراه هوش مصنوعی است که در خانه شما زندگی می‌کند. 
تنظیم آن تنها در چند دقیقه امکان‌پذیر است. نیازی به کدنویسی نیست.

این هوش مصنوعی دنیای واقعی را از طریق دوربین‌ها درک می‌کند، بر روی بدنه ربات حرکت می‌کند، با صدای بلند صحبت می‌کند و آنچه را که می‌بیند به خاطر می‌سپارد. به آن یک نام بدهید، شخصیت آن را بنویسید و اجازه دهید با شما زندگی کند.

## چه کارهایی می‌تواند انجام دهد

- 👁 **ببیند** — تصاویر را از یک دوربین PTZ وای‌فای یا وب‌کم USB ضبط می‌کند
- 🔄 **دور بزند** — دوربین را به آرامی حرکت می‌دهد تا محیط اطرافش را کشف کند
- 🦿 **حرکت کند** — یک ربات جاروبرقی را به حرکت در می‌آورد
- 🗣 **صحبت کند** — از طریق ElevenLabs TTS صحبت می‌کند
- 🎙 **گوش کند** — ورودی صوتی بدون دست از طریق ElevenLabs Realtime STT (نیاز به فعال‌سازی)
- 🧠 **به خاطر بسپارد** — به طور فعال خاطرات را با جستجوی معنایی ذخیره و بازیابی می‌کند (SQLite + embeddings)
- 🫀 **نظریه ذهن** — قبل از پاسخ دادن، دیدگاه شخص دیگر را در نظر می‌گیرد
- 💭 **خواسته** — دارای محرک‌های داخلی خود است که رفتار خودمختار را تحریک می‌کند

## چگونه کار می‌کند

familiar-ai یک حلقه [ReAct](https://arxiv.org/abs/2210.03629) را با انتخاب شما از LLM اجرا می‌کند. آن دنیای اطراف را از طریق ابزارها درک می‌کند، درباره قدم بعدی فکر می‌کند و عمل می‌کند — دقیقاً مانند یک انسان.

```
user input
  → think → act ( دوربین / حرکت / صحبت / به یاد آوردن) → مشاهده → فکر → ...
```

وقتی بیکار است، بر اساس خواسته‌های خود عمل می‌کند: کنجکاوی، خواستن برای نگاه کردن به خارج، دلتنگی برای شخصی که با او زندگی می‌کند.

## شروع

### 1. نصب uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. نصب ffmpeg

ffmpeg **مورد نیاز** برای ضبط تصویر دوربین و پخش صوت است.

| OS | فرمان |
|----|---------|
| macOS | `brew install ffmpeg` |
| اوبونتو / دبیان | `sudo apt install ffmpeg` |
| فدورا / RHEL | `sudo dnf install ffmpeg` |
| آرچ لینوکس | `sudo pacman -S ffmpeg` |
| ویندوز | `winget install ffmpeg` — یا از [ffmpeg.org](https://ffmpeg.org/download.html) دانلود کنید و به PATH اضافه کنید |
| Raspberry Pi | `sudo apt install ffmpeg` |

تایید کنید: `ffmpeg -version`

### 3. کلون و نصب

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. پیکربندی

```bash
cp .env.example .env
# .env را با تنظیمات خود ویرایش کنید
```

**حداقل موارد مورد نیاز:**

| متغیر | توضیحات |
|----------|-------------|
| `PLATFORM` | `anthropic` (پیش‌فرض) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | کلید API شما برای پلتفرم انتخابی |

**اختیاری:**

| متغیر | توضیحات |
|----------|-------------|
| `MODEL` | نام مدل (پیش‌فرض‌های مناسب برای هر پلتفرم) |
| `AGENT_NAME` | نام نمایشی که در TUI نشان داده می‌شود (مثلاً `Yukine`) |
| `CAMERA_HOST` | آدرس IP دوربین ONVIF/RTSP شما |
| `CAMERA_USER` / `CAMERA_PASS` | اطلاعات اعتبار دوربین |
| `ELEVENLABS_API_KEY` | برای خروجی صوت — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true` برای فعال‌سازی ورودی صوتی همیشه‌فعال بدون دست (نیاز به `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | محل پخش صوت: `local` (بلندگوی PC، پیش‌فرض) \| `remote` (بلندگوی دوربین) \| `both` |
| `THINKING_MODE` | فقط Anthropic — `auto` (پیش‌فرض) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | تلاش فکری تطبیقی: `high` (پیش‌فرض) \| `medium` \| `low` \| `max` (فقط Opus 4.6) |

### 5. ساختن آشنای خود

```bash
cp persona-template/en.md ME.md
# ME.md را ویرایش کنید — به آن یک نام و شخصیت بدهید
```

### 6. اجرا

```bash
./run.sh             # TUI متنی (توصیه شده)
./run.sh --no-tui    # REPL ساده
```

---

## انتخاب یک LLM

> **توصیه شده: Kimi K2.5** — بهترین عملکرد عامل محوری که تاکنون آزمایش شده است. به زمینه توجه می‌کند، سوالات پیگیری می‌پرسد و به صورت خودمختار عمل می‌کند به گونه‌ای که سایر مدل‌ها نمی‌کنند. قیمت‌گذاری مشابه Claude Haiku دارد.

| پلتفرم | `PLATFORM=` | مدل پیش‌فرض | کجا کلید را بگیرید |
|----------|------------|---------------|-----------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| OpenAI-compatible (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (چند-تأمین‌کننده) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **ابزار CLI** (claude -p, ollama…) | `cli` | (فرمان) | — |

**مثال `.env` Kimi K2.5:**
```env
PLATFORM=kimi
API_KEY=sk-...   # از platform.moonshot.ai
AGENT_NAME=Yukine
```

**مثال `.env` Z.AI GLM:**
```env
PLATFORM=glm
API_KEY=...   # از api.z.ai
MODEL=glm-4.6v   # فعال‌سازی بصری؛ glm-4.7 / glm-5 = فقط متن
AGENT_NAME=Yukine
```

**مثال `.env` Google Gemini:**
```env
PLATFORM=gemini
API_KEY=AIza...   # از aistudio.google.com
MODEL=gemini-2.5-flash  # یا gemini-2.5-pro برای قابلیت بالاتر
AGENT_NAME=Yukine
```

**مثال `.env` OpenRouter.ai:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   # از openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # اختیاری: مدل را مشخص کنید
AGENT_NAME=Yukine
```

> **توجه:** برای غیر فعال کردن مدل‌های محلی/NVIDIA، به سادگی `BASE_URL` را به یک نقطه انتهایی محلی مانند `http://localhost:11434/v1` تنظیم نکنید. به جای آن از تأمین‌کنندگان ابری استفاده کنید.

**مثال `.env` ابزار CLI:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # llm CLI (https://llm.datasette.io) — {} = آرگومان درخواست
# MODEL=ollama run gemma3:27b  # Ollama — بدون {}, درخواست از طریق stdin می‌رود
```

---

## سرورهای MCP

familiar-ai می‌تواند به هر [سرور MCP (پروتکل زمینه مدل)](https://modelcontextprotocol.io) متصل شود. این به شما امکان می‌دهد حافظه خارجی، دسترسی به سیستم فایل، جستجوی وب یا هر ابزار دیگری را وصل کنید.

سرورها را در `~/.familiar-ai.json` پیکربندی کنید (فرمت مشابه Claude Code):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

دو نوع حمل‌ونقل پشتیبانی می‌شود:
- **`stdio`**: راه‌اندازی یک زیر فرآیند محلی (`command` + `args`)
- **`sse`**: اتصال به یک سرور HTTP+SSE (`url`)

مکان فایل تنظیمات را با `MCP_CONFIG=/path/to/config.json` نادیده بگیرید.

---

## سخت‌افزار

familiar-ai با هر سخت‌افزاری که دارید — یا اصلاً سخت‌افزاری — کار می‌کند.

| قطعه | چه کاری انجام می‌دهد | مثال | ضروری؟ |
|------|-------------|---------|-----------|
| دوربین PTZ وای‌فای | چشم‌ها + گردن | Tapo C220 (~$30) | **توصیه شده** |
| وب‌کم USB | چشم‌ها (ثابت) | هر دوربین UVC | **توصیه شده** |
| جاروبرقی ربات | پاها | هر مدل سازگار با Tuya | خیر |
| PC / Raspberry Pi | مغز | هر چیزی که Python را اجرا کند | **بله** |

> **وجود یک دوربین به شدت توصیه می‌شود.** بدون آن، familiar-ai می‌تواند صحبت کند — اما نمی‌تواند دنیای اطراف را ببیند که اصولاً هدف اصلی آن است.

### تنظیم حداقلی (بدون سخت‌افزار)

فقط می‌خواهید امتحان کنید؟ شما فقط به یک کلید API نیاز دارید:

```env
PLATFORM=kimi
API_KEY=sk-...
```

`./run.sh` را اجرا کنید و شروع به گفتگو کنید. سخت‌افزار را در حین حرکت اضافه کنید.

### دوربین PTZ وای‌فای (Tapo C220)

1. در برنامه Tapo: **تنظیمات → پیشرفته → حساب دوربین** — یک حساب محلی ایجاد کنید (نه حساب TP-Link)
2. IP دوربین را در لیست دستگاه‌های روتر خود پیدا کنید
3. در `.env` تنظیم کنید:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=your-local-user
   CAMERA_PASS=your-local-pass
   ```

### صدا (ElevenLabs)

1. یک کلید API در [elevenlabs.io](https://elevenlabs.io/) بگیرید
2. در `.env` تنظیم کنید:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # اختیاری، از صدای پیش‌فرض استفاده می‌کند اگر حذف شود
   ```

دو مقصد پخش وجود دارد که توسط `TTS_OUTPUT` کنترل می‌شود:

```env
TTS_OUTPUT=local    # بلندگوی PC (پیش‌فرض)
TTS_OUTPUT=remote   # فقط بلندگوی دوربین
TTS_OUTPUT=both     # بلندگوی دوربین + بلندگوی PC به طور همزمان
```

#### A) بلندگوی دوربین (از طریق go2rtc)

`TTS_OUTPUT=remote` (یا `both`) را تنظیم کنید. نیاز به [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. باینری را از [صفحه تولیدات](https://github.com/AlexxIT/go2rtc/releases) دانلود کنید:
   - لینوکس/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **ویندوز: `go2rtc_win64.exe`**

2. آن را جاگذاری و نام‌گذاری کنید:
   ```
   # لینوکس / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # chmod +x مورد نیاز است

   # ویندوز
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. `go2rtc.yaml` را در همان دایرکتوری ایجاد کنید:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   از اطلاعات اعتبار حساب محلی دوربین استفاده کنید (نه حساب کلود TP-Link خود).

4. familiar-ai به طور خودکار در زمان راه‌اندازی go2rtc را شروع می‌کند. اگر دوربین شما از صدای دو طرفه (backchannel) پشتیبانی کند، صدا از بلندگوی دوربین پخش می‌شود.

#### B) بلندگوی محلی PC

پیش‌فرض (`TTS_OUTPUT=local`). سعی می‌کند پخش‌کننده‌ها را به ترتیب انتخاب کند: **paplay** → **mpv** → **ffplay**. همچنین به عنوان پشتیبان زمانی که `TTS_OUTPUT=remote` و go2rtc در دسترس نیست استفاده می‌شود.

| OS | نصب |
|----|---------|
| macOS | `brew install mpv` |
| اوبونتو / دبیان | `sudo apt install mpv` (یا `paplay` از طریق `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — در `.env` `PULSE_SERVER=unix:/mnt/wslg/PulseServer` را تنظیم کنید |
| ویندوز | [mpv.io/installation](https://mpv.io/installation/) — دانلود کرده و به PATH اضافه کنید، **یا** `winget install ffmpeg` |

> اگر هیچ پخش کننده صوتی در دسترس نیست، گفتار هنوز تولید می‌شود — اما پخش نمی‌شود.

### ورودی صدا (Realtime STT)

`REALTIME_STT=true` را در `.env` تنظیم کنید برای ورودی صوتی همیشه‌فعال و بدون دست:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # همان کلید به عنوان TTS
```

familiar-ai صوت میکروفن را به ElevenLabs Scribe v2 استریم کرده و به صورت خودکار متون را زمانی که شما صحبت را متوقف می‌کنید، ثبت می‌کند. نیازی به فشار دکمه نیست. با حالت فشار برای صحبت (Ctrl+T) همزیستی دارد.

---

## TUI

familiar-ai شامل یک رابط کاربری ترمینال است که با [Textual](https://textual.textualize.io/) ساخته شده است:

- تاریخچه گفتگوی قابل پیمایش با متن زنده
- تکمیل خودکار برای `/quit`، `/clear`
- جلوگیری از عامل در حین تفکر با تایپ کردن در حین تفکر
- **گزارش گفت‌و‌گو** به طور خودکار به `~/.cache/familiar-ai/chat.log` ذخیره می‌شود

برای پیگیری لاگ در ترمینال دیگری (برای کپی-چسباندن مفید):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## شخصیت (ME.md)

شخصیت آشنای شما در `ME.md` زندگی می‌کند. این فایل در git نادیده گرفته می‌شود — فقط متعلق به شماست.

برای مثال به [`persona-template/en.md`](./persona-template/en.md) نگاهی بیندازید، یا برای نسخه ژاپنی به [`persona-template/ja.md`](./persona-template/ja.md) مراجعه کنید.

---

## سوالات متداول

**س: آیا بدون GPU کار می‌کند؟**
بله. مدل embedding (multilingual-e5-small) به خوبی بر روی CPU کار می‌کند. GPU آن را سریع‌تر می‌کند اما ضروری نیست.

**س: آیا می‌توانم از دوربینی غیر از Tapo استفاده کنم؟**
هر دوربینی که از ONVIF + RTSP پشتیبانی کند باید کار کند. ما با Tapo C220 آزمایش کرده‌ایم.

**س: آیا داده‌های من به جایی ارسال می‌شود؟**
تصاویر و متن‌ها برای پردازش به API LLM انتخابی شما ارسال می‌شوند. خاطرات به طور محلی در `~/.familiar_ai/` ذخیره می‌شوند.

**س: چرا عامل به جای صحبت کردن `（...）` می‌نویسد؟**
مطمئن شوید که `ELEVENLABS_API_KEY` تنظیم شده است. بدون آن، صدای خروجی غیر فعال می‌شود و عامل به متن باز می‌گردد.

## پس‌زمینه فنی

کنجکاوید درباره چگونگی کار آن؟ برای تحقیقات و تصمیمات طراحی پشت familiar-ai به [docs/technical.md](./docs/technical.md) مراجعه کنید — ReAct، SayCan، Reflexion، Voyager، سیستم خواسته و بیشتر.

---

## مشارکت

familiar-ai یک آزمایش باز است. اگر هر یک از این موارد با شما هم reson می‌دهد — از نظر فنی یا فلسفی — مشارکت‌ها بسیار خوش‌آمد است.

**جای‌های خوب برای شروع:**

| حوزه | چه چیزی مورد نیاز است |
|------|---------------|
| سخت‌افزار جدید | پشتیبانی از دوربین‌های بیشتر (RTSP، وب‌کم IP)، میکروفون‌ها، عملگرها |
| ابزارهای جدید | جستجوی وب، اتوماسیون خانگی، تقویم، هر چیزی از طریق MCP |
| بک‌اندهای جدید | هر LLM یا مدل محلی که با رابط `stream_turn` سازگار باشد |
| قالب‌های شخصیت | الگوهای ME.md برای زبان‌ها و شخصیت‌های مختلف |
| تحقیق | مدل‌های خواسته بهتر، بازیابی حافظه، تحریک نظریه ذهن |
| مستندسازی | آموزش‌ها، راهنماها، ترجمه‌ها |

به [CONTRIBUTING.md](./CONTRIBUTING.md) برای راه‌اندازی توسعه، سبک کد و دستورالعمل‌های PR مراجعه کنید.

اگر نمی‌دانید از کجا شروع کنید، [یک مشکل باز کنید](https://github.com/lifemate-ai/familiar-ai/issues) — خوشحال می‌شوم به شما راهنمایی کنم.

---

## مجوز

[MIT](./LICENSE)
```
