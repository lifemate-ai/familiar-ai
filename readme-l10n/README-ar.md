```markdown
# familiar-ai 🐾

**ذكاء اصطناعي يعيش معك** — بعينين، وصوت، وأرجل، وذاكرة.

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai هو رفيق ذكاء اصطناعي يعيش في منزلك.
قم بإعداده في دقائق. لا حاجة للبرمجة.

إنه يدرك العالم الحقيقي من خلال الكاميرات، ويتحرك على جسم روبوت، ويتحدث بصوت عالٍ، ويتذكر ما يراه. أعطه اسمًا، واكتب شخصيته، ودعه يعيش معك.

## ماذا يمكن أن يفعل

- 👁 **يرى** — يلتقط الصور من كاميرا PTZ Wi-Fi أو كاميرا USB
- 🔄 **يتجول** — يحرك الكاميرا يمينًا ويسارًا لاستكشاف محيطه
- 🦿 **يتحرك** — يقود مكنسة كهربائية روبوتية للتجول في الغرفة
- 🗣 **يتحدث** — يتحدث عبر ElevenLabs TTS
- 🎙 **يستمع** — إدخال صوت بدون استخدام اليدين عبر ElevenLabs Realtime STT (اختياري)
- 🧠 **يتذكر** — يخزن ويسترجع الذكريات بنشاط باستخدام بحث دلالي (SQLite + embeddings)
- 🫀 **نظرية العقل** — يأخذ وجهة نظر الشخص الآخر قبل الرد
- 💭 **رغبة** — لديه دوافع داخلية تؤدي إلى سلوك مستقل

## كيف يعمل

familiar-ai يعمل على حلقة [ReAct](https://arxiv.org/abs/2210.03629) مدعومة باختيارك من LLM. إنه يدرك العالم من خلال الأدوات، ويفكر في ما سيفعله بعد ذلك، ويتصرف — تمامًا كما يفعل الشخص.

```
user input
  → think → act (camera / move / speak / remember) → observe → think → ...
```

عندما يكون خاملاً، يتصرف وفقًا لرغباته: الفضول، ورغبة في النظر للخارج، والشعور بالافتقاد للشخص الذي يعيش معه.

## البدء

### 1. تثبيت uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. تثبيت ffmpeg

ffmpeg **مطلوب** لالتقاط صور الكاميرا وتشغيل الصوت.

| OS | الأمر |
|----|---------|
| macOS | `brew install ffmpeg` |
| Ubuntu / Debian | `sudo apt install ffmpeg` |
| Fedora / RHEL | `sudo dnf install ffmpeg` |
| Arch Linux | `sudo pacman -S ffmpeg` |
| Windows | `winget install ffmpeg` — أو تنزيله من [ffmpeg.org](https://ffmpeg.org/download.html) وإضافته إلى PATH |
| Raspberry Pi | `sudo apt install ffmpeg` |

تحقق: `ffmpeg -version`

### 3. استنساخ وتثبيت

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. إعداد

```bash
cp .env.example .env
# قم بتحرير .env باستخدام الإعدادات الخاصة بك
```

**المطلوب كحد أدنى:**

| المتغير | الوصف |
|----------|-------------|
| `PLATFORM` | `anthropic` (افتراضي) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | مفتاح API الخاص بك على المنصة المختارة |

**اختياري:**

| المتغير | الوصف |
|----------|-------------|
| `MODEL` | اسم النموذج (افتراضات معقولة لكل منصة) |
| `AGENT_NAME` | اسم العرض الذي يظهر في TUI (مثل `Yukine`) |
| `CAMERA_HOST` | عنوان IP الخاص بكاميرا ONVIF/RTSP |
| `CAMERA_USER` / `CAMERA_PASS` | بيانات اعتماد الكاميرا |
| `ELEVENLABS_API_KEY` | لخرج الصوت — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true` لتمكين إدخال الصوت بدون استخدام اليدين (يتطلب `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | مكان تشغيل الصوت: `local` (سماعة الكمبيوتر، الافتراضي) \| `remote` (سماعة الكاميرا) \| `both` |
| `THINKING_MODE` | Anthropic فقط — `auto` (افتراضي) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | جهد التفكير التكيفي: `high` (افتراضي) \| `medium` \| `low` \| `max` (فقط Opus 4.6) |

### 5. أنشئ الرفيق الخاص بك

```bash
cp persona-template/en.md ME.md
# قم بتحرير ME.md — أعطه اسمًا وشخصية
```

### 6. تشغيل

```bash
./run.sh             # واجهة نصية TUI (موصى بها)
./run.sh --no-tui    # REPL عادي
```

---

## اختيار LLM

> **موصى به: Kimi K2.5** — أفضل أداء وكيل تم اختباره حتى الآن. يلاحظ السياق، ويطرح أسئلة متابعة، ويتصرف بشكل مستقل بطرق لا تفعلها النماذج الأخرى. السعر مشابه لـ Claude Haiku.

| المنصة | `PLATFORM=` | النموذج الافتراضي | أين تحصل على المفتاح |
|----------|------------|---------------|-----------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| متوافق مع OpenAI (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (متعدد الموفرين) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **أداة CLI** (claude -p، ollama…) | `cli` | (الأمر) | — |

**مثال `.env` لـ Kimi K2.5:**
```env
PLATFORM=kimi
API_KEY=sk-...   # من platform.moonshot.ai
AGENT_NAME=Yukine
```

**مثال `.env` لـ Z.AI GLM:**
```env
PLATFORM=glm
API_KEY=...   # من api.z.ai
MODEL=glm-4.6v   # مدعوم بالرؤية؛ glm-4.7 / glm-5 = نص فقط
AGENT_NAME=Yukine
```

**مثال `.env` لـ Google Gemini:**
```env
PLATFORM=gemini
API_KEY=AIza...   # من aistudio.google.com
MODEL=gemini-2.5-flash  # أو gemini-2.5-pro لقدرات أعلى
AGENT_NAME=Yukine
```

**مثال `.env` لـ OpenRouter.ai:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   # من openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # اختياري: حدد النموذج
AGENT_NAME=Yukine
```

> **ملاحظة:** لتعطيل النماذج المحلية/NVIDIA، لا تقم فقط بتعيين `BASE_URL` على نقطة نهاية محلية مثل `http://localhost:11434/v1`. استخدم مزودي الخدمات السحابية بدلاً من ذلك.

**مثال `.env` لأداة CLI:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # واجهة سطر الأوامر llm (https://llm.datasette.io) — {} = برمجة الإرشاد
# MODEL=ollama run gemma3:27b  # Ollama — لا {}، الانتقال عبر stdin
```

---

## خوادم MCP

يمكن لـ familiar-ai الاتصال بأي خادم [MCP (بروتوكول سياق النموذج)](https://modelcontextprotocol.io). هذا يسمح لك بتوصيل الذاكرة الخارجية، والوصول إلى نظام الملفات، والبحث عبر الويب، أو أي أداة أخرى.

قم بتكوين الخوادم في `~/.familiar-ai.json` (بنفس تنسيق كود كلود):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

يدعم نوعان من النقل:
- **`stdio`**: إطلاق عملية فرعية محلية (`command` + `args`)
- **`sse`**: الاتصال بخادم HTTP+SSE (`url`)

تجاوز موقع ملف التكوين باستخدام `MCP_CONFIG=/path/to/config.json`.

---

## الأجهزة

يعمل familiar-ai مع أي جهاز لديك — أو بدون أي منها.

| الجزء | ماذا يفعل | مثال | مطلوب؟ |
|------|-------------|---------|-----------|
| كاميرا PTZ Wi-Fi | عيون + رقبة | Tapo C220 (~$30) | **موصى بها** |
| كاميرا USB | عيون (ثابتة) | أي كاميرا UVC | **موصى بها** |
| مكنسة كهربائية روبوت | أرجل | أي نموذج متوافق مع Tuya | لا |
| كمبيوتر شخصي / Raspberry Pi | دماغ | أي شيء يعمل بنظام Python | **نعم** |

> **يوصى بشدة باستخدام كاميرا.** بدون واحدة، يمكن لـ familiar-ai التحدث — لكنه لا يمكنه رؤية العالم، وهذا هو المعنى الحقيقي لذلك.

### الإعداد الأدنى (بدون أجهزة)

هل تريد فقط تجربته؟ تحتاج فقط إلى مفتاح API:

```env
PLATFORM=kimi
API_KEY=sk-...
```

قم بتشغيل `./run.sh` وابدأ الدردشة. أضف الأجهزة حسب الحاجة.

### كاميرا PTZ Wi-Fi (Tapo C220)

1. في تطبيق Tapo: **الإعدادات → متقدم → حساب الكاميرا** — قم بإنشاء حساب محلي (ليس حساب TP-Link)
2. ابحث عن عنوان IP للكاميرا في قائمة الأجهزة لديك في جهاز التوجيه
3. قم بتعيينه في `.env`:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=your-local-user
   CAMERA_PASS=your-local-pass
   ```

### الصوت (ElevenLabs)

1. احصل على مفتاح API من [elevenlabs.io](https://elevenlabs.io/)
2. قم بتعيينه في `.env`:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # اختياري، يستخدم الصوت الافتراضي إذا تم تجاهله
   ```

هناك وجهتان للتشغيل، يتم التحكم فيهما بواسطة `TTS_OUTPUT`:

```env
TTS_OUTPUT=local    # سماعة الكمبيوتر (افتراضي)
TTS_OUTPUT=remote   # سماعة الكاميرا فقط
TTS_OUTPUT=both     # سماعة الكاميرا + سماعة الكمبيوتر في نفس الوقت
```

#### أ) سماعة الكاميرا (عبر go2rtc)

تعيين `TTS_OUTPUT=remote` (أو `both`). يتطلب [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. تنزيل البرنامج من [صفحة الإصدارات](https://github.com/AlexxIT/go2rtc/releases):
   - Linux/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **Windows: `go2rtc_win64.exe`**

2. ضعها وأعد تسميتها:
   ```
   # Linux / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # يتطلب chmod +x

   # Windows
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. أنشئ `go2rtc.yaml` في نفس الدليل:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   استخدم بيانات اعتماد حساب الكاميرا المحلي (ليس حساب TP-Link السحابي).

4. يقوم familiar-ai بتشغيل go2rtc تلقائيًا عند الإطلاق. إذا كانت الكاميرا تدعم الصوت ثنائي الاتجاه (قناة خلفية)، فسيتم تشغيل الصوت من سماعة الكاميرا.

#### ب) سماعة الكمبيوتر المحلية

الإعداد الافتراضي (`TTS_OUTPUT=local`). يحاول تشغيل المشغلين بالترتيب: **paplay** → **mpv** → **ffplay**. يتم استخدامه أيضًا كخيار احتياطي عندما يكون `TTS_OUTPUT=remote` و go2rtc غير متاح.

| OS | التثبيت |
|----|---------|
| macOS | `brew install mpv` |
| Ubuntu / Debian | `sudo apt install mpv` (أو `paplay` عبر `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — تعيين `PULSE_SERVER=unix:/mnt/wslg/PulseServer` في `.env` |
| Windows | [mpv.io/installation](https://mpv.io/installation/) — تنزيله وإضافته إلى PATH، **أو** `winget install ffmpeg` |

> إذا لم يكن هناك لاعب صوتي متاح، لا يزال يتم إنشاء الكلام — إلا أنه لن يتم تشغيله.

### إدخال الصوت (Realtime STT)

قم بتعيين `REALTIME_STT=true` في `.env` لتفعيل الإدخال الصوتي دائماً، بدون استخدام اليدين:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # نفس المفتاح مثل TTS
```

يقوم familiar-ai ببث صوت الميكروفون إلى ElevenLabs Scribe v2 ويقوم تلقائيًا بتجميع النصوص عندما تتوقف عن الكلام. لا حاجة للضغط على زر. يت coexist مع وضع الدفع للحديث (Ctrl+T).

---

## TUI

يتضمن familiar-ai واجهة محمولة نصية مصممة باستخدام [Textual](https://textual.textualize.io/):

- تاريخ محادثة قابل للتمرير مع نص مباشر
- إكمال تلقائي للأوامر `/quit`, `/clear`
- مقاطعة الوكيل في منتصف الدور من خلال الكتابة أثناء تفكيره
- **سجل المحادثة** يتم حفظه تلقائيًا في `~/.cache/familiar-ai/chat.log`

لمتابعة السجل في نافذة طرفية أخرى (مفيدة للنسخ واللصق):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## الشخصية (ME.md)

تعيش شخصية رفيقك في `ME.md`. هذا الملف غير محمل في git — إنه ملك لك وحدك.

اطلع على [`persona-template/en.md`](./persona-template/en.md) كمثال، أو [`persona-template/ja.md`](./persona-template/ja.md) كنسخة يابانية.

---

## الأسئلة المتكررة

**س: هل يعمل بدون GPU؟**
نعم. يعمل نموذج التضمين (multilingual-e5-small) بشكل جيد على المعالج. يجعل GPU الأمر أسرع ولكنه ليس مطلوبًا.

**س: هل يمكنني استخدام كاميرا غير Tapo؟**
أي كاميرا تدعم ONVIF + RTSP يجب أن تعمل. تم اختبار Tapo C220.

**س: هل يتم إرسال بياناتي إلى أي مكان؟**
يتم إرسال الصور والنصوص إلى واجهة API المختارة لمعالجتها. يتم تخزين الذكريات محليًا في `~/.familiar_ai/`.

**س: لماذا يكتب الوكيل `（...）` بدلاً من التحدث؟**
تأكد من تعيين `ELEVENLABS_API_KEY`. بدونه، يتم تعطيل الصوت ويعود الوكيل إلى النص.

## الخلفية التقنية

هل أنت فضولي بشأن كيفية عمله؟ اطلع على [docs/technical.md](./docs/technical.md) للبحث وقرارات التصميم وراء familiar-ai — ReAct، SayCan، Reflexion، Voyager، نظام الرغبة، والمزيد.

---

## المساهمة

familiar-ai تجربة مفتوحة. إذا كان أي من هذا يت reson معك — تقنيًا أو فلسفيًا — فإن المساهمات مرحب بها بشدة.

**أماكن جيدة للبدء:**

| المنطقة | ما هو مطلوب |
|------|---------------|
| الأجهزة الجديدة | دعم لمزيد من الكاميرات (RTSP، كاميرا IP)، ميكروفونات، مشغلات |
| أدوات جديدة | بحث عبر الويب، أتمتة المنزل، تقويم، أي شيء عبر MCP |
| الواجهات الخلفية الجديدة | أي LLM أو نموذج محلي يناسب واجهة `stream_turn` |
| قوالب الشخصية | قوالب ME.md للغات وشخصيات مختلفة |
| البحث | نماذج رغبة أفضل، استرجاع الذاكرة، تحفيز نظرية العقل |
| الوثائق | دروس، جولات، ترجمات |

اطلع على [CONTRIBUTING.md](./CONTRIBUTING.md) لإعداد التطوير، وأسلوب الشيفرة، وإرشادات PR.

إذا كنت غير متأكد من أين تبدأ، [افتتح قضية](https://github.com/lifemate-ai/familiar-ai/issues) — سأكون سعيدًا بإرشادك في الاتجاه الصحيح.

---

## الترخيص

[MIT](./LICENSE)
```
