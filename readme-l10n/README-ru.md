```markdown
# familiar-ai 🐾

**Искусственный интеллект, который живет рядом с вами** — с глазами, голосом, ногами и памятью.

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai это AI-компаньон, который живет в вашем доме.
Настроить его можно за считанные минуты. Кодировать не требуется.

Он воспринимает реальный мир через камеры, перемещается на теле робота, говорит вслух и запоминает то, что видит. Дайте ему имя, напишите его личность и позвольте ему жить с вами.

## Что он может делать

- 👁 **Видеть** — захватывает изображения с Wi-Fi PTZ камеру или USB веб-камеры
- 🔄 **Оглядеть** — поворачивает и наклоняет камеру, чтобы исследовать окружающую среду
- 🦿 **Двигаться** — управляет роботом-пылесосом по комнате
- 🗣 **Говорить** — разговаривает через ElevenLabs TTS
- 🎙 **Слушать** — ввод голоса без рук через ElevenLabs Realtime STT (по желанию)
- 🧠 **Помнить** — активно хранит и вспоминает воспоминания с помощью семантического поиска (SQLite + эмбеддинги)
- 🫀 **Теория разума** — принимает точку зрения другого человека перед ответом
- 💭 **Желание** — имеет свои внутренние стимулы, которые вызывают автономное поведение

## Как это работает

familiar-ai запускает цикл [ReAct](https://arxiv.org/abs/2210.03629), управляемый вашим выбором LLM. Он воспринимает мир через инструменты, думает о том, что делать дальше, и действует — как и человек.

```
user input
  → think → act (camera / move / speak / remember) → observe → think → ...
```

Когда он бездействует, он действует на основе своих желаний: любопытства, желания посмотреть на улицу, скучая по человеку, с которым он живет.

## Начало работы

### 1. Установите uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. Установите ffmpeg

ffmpeg является **обязательным** для захвата изображения с камеры и воспроизведения звука.

| ОС | Команда |
|----|---------|
| macOS | `brew install ffmpeg` |
| Ubuntu / Debian | `sudo apt install ffmpeg` |
| Fedora / RHEL | `sudo dnf install ffmpeg` |
| Arch Linux | `sudo pacman -S ffmpeg` |
| Windows | `winget install ffmpeg` — или скачайте с [ffmpeg.org](https://ffmpeg.org/download.html) и добавьте в PATH |
| Raspberry Pi | `sudo apt install ffmpeg` |

Проверьте: `ffmpeg -version`

### 3. Клонируйте и установите

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. Настройка

```bash
cp .env.example .env
# Отредактируйте .env с вашими настройками
```

**Минимально необходимое:**

| Переменная | Описание |
|------------|-------------|
| `PLATFORM` | `anthropic` (по умолчанию) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | Ваш API ключ для выбранной платформы |

**Опционально:**

| Переменная | Описание |
|------------|-------------|
| `MODEL` | Имя модели (разумные значения по умолчанию для каждой платформы) |
| `AGENT_NAME` | Отображаемое имя, показываемое в TUI (например, `Yukine`) |
| `CAMERA_HOST` | IP адрес вашей ONVIF/RTSP камеры |
| `CAMERA_USER` / `CAMERA_PASS` | Учетные данные камеры |
| `ELEVENLABS_API_KEY` | Для голосового вывода — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true`, чтобы активировать голосовой ввод без рук (требует `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | Где воспроизводить звук: `local` (динамик ПК, по умолчанию) \| `remote` (динамик камеры) \| `both` |
| `THINKING_MODE` | Только Anthropic — `auto` (по умолчанию) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | Адаптивные усилия при размышлении: `high` (по умолчанию) \| `medium` \| `low` \| `max` (только Opus 4.6) |

### 5. Создайте вашего знакомого

```bash
cp persona-template/en.md ME.md
# Отредактируйте ME.md — дайте ему имя и личность
```

### 6. Запустите

```bash
./run.sh             # Текстовый TUI (рекомендуется)
./run.sh --no-tui    # Обычный REPL
```

---

## Выбор LLM

> **Рекомендуется: Kimi K2.5** — лучшая агентовая производительность, протестированная до сих пор. Учитывает контекст, задает дополнительные вопросы и действует автономно, как другие модели. Ценовая категория аналогична Claude Haiku.

| Платформа | `PLATFORM=` | Модель по умолчанию | Где получить ключ |
|-----------|-------------|---------------------|-------------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| Совместимый с OpenAI (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (мультипровайдер) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **Инструмент CLI** (claude -p, ollama…) | `cli` | (команда) | — |

**Пример `.env` для Kimi K2.5:**
```env
PLATFORM=kimi
API_KEY=sk-...   # с platform.moonshot.ai
AGENT_NAME=Yukine
```

**Пример `.env` для Z.AI GLM:**
```env
PLATFORM=glm
API_KEY=...   # с api.z.ai
MODEL=glm-4.6v   # с поддержкой зрения; glm-4.7 / glm-5 = только текст
AGENT_NAME=Yukine
```

**Пример `.env` для Google Gemini:**
```env
PLATFORM=gemini
API_KEY=AIza...   # с aistudio.google.com
MODEL=gemini-2.5-flash  # или gemini-2.5-pro для большей мощности
AGENT_NAME=Yukine
```

**Пример `.env` для OpenRouter.ai:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   # с openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # необязательно: укажите модель
AGENT_NAME=Yukine
```

> **Примечание:** Чтобы отключить локальные/NVIDIA модели, просто не устанавливайте `BASE_URL` на локальную конечную точку, например, `http://localhost:11434/v1`. Используйте облачных провайдеров вместо этого.

**Пример `.env` для инструмента CLI:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # llm CLI (https://llm.datasette.io) — {} = аргумент запроса
# MODEL=ollama run gemma3:27b  # Ollama — без {}, приглашение идет через stdin
```

---

## Серверы MCP

familiar-ai может подключаться к любому серверу [MCP (Model Context Protocol)](https://modelcontextprotocol.io). Это позволяет вам подключить внешнюю память, доступ к файловой системе, веб-поиск или любые другие инструменты.

Настройте серверы в `~/.familiar-ai.json` (такой же формат, как у Claude Code):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

Поддерживаются два типа транспортировки:
- **`stdio`**: запуск локального подпроцесса (`command` + `args`)
- **`sse`**: подключение к серверу HTTP+SSE (`url`)

Переопределите местоположение конфигурационного файла с помощью `MCP_CONFIG=/path/to/config.json`.

---

## Аппаратное обеспечение

familiar-ai работает с любым аппаратным обеспечением, которое у вас есть — или вообще без него.

| Часть | Что она делает | Пример | Обязательно? |
|-------|----------------|--------|--------------|
| Wi-Fi PTZ камера | Глаза + шея | Tapo C220 (~$30) | **Рекомендуется** |
| USB веб-камера | Глаза (фиксированные) | Любая UVC камера | **Рекомендуется** |
| Робот-пылесос | Ноги | Любая совместимая модель Tuya | Нет |
| ПК / Raspberry Pi | Мозг | Все, что работает на Python | **Да** |

> **Камера настоятельно рекомендуется.** Без нее familiar-ai все еще может говорить — но он не может видеть мир, что как бы и есть суть.

### Минимальная настройка (без аппаратного обеспечения)

Хотите просто попробовать? Вам нужен только API-ключ:

```env
PLATFORM=kimi
API_KEY=sk-...
```

Запустите `./run.sh` и начните общаться. Добавляйте оборудование по мере необходимости.

### Wi-Fi PTZ камера (Tapo C220)

1. В приложении Tapo: **Настройки → Расширенные → Учетная запись камеры** — создайте локальную учетную запись (не TP-Link учетная запись)
2. Найдите IP-адрес камеры в списке устройств вашего маршрутизатора
3. Установите в `.env`:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=your-local-user
   CAMERA_PASS=your-local-pass
   ```

### Голос (ElevenLabs)

1. Получите API-ключ на [elevenlabs.io](https://elevenlabs.io/)
2. Установите в `.env`:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # необязательно, используется голос по умолчанию, если не указано
   ```

Есть два места воспроизведения, управляемых `TTS_OUTPUT`:

```env
TTS_OUTPUT=local    # Динамик ПК (по умолчанию)
TTS_OUTPUT=remote   # Только динамик камеры
TTS_OUTPUT=both     # Динамик камеры + Динамик ПК одновременно
```

#### A) Динамик камеры (через go2rtc)

Установите `TTS_OUTPUT=remote` (или `both`). Требует [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. Скачайте бинарник со [страницы релизов](https://github.com/AlexxIT/go2rtc/releases):
   - Linux/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **Windows: `go2rtc_win64.exe`**

2. Поместите и переименуйте его:
   ```
   # Linux / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # требуются права на выполнение

   # Windows
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. Создайте `go2rtc.yaml` в той же директории:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   Используйте учетные данные локальной учетной записи камеры (не свою учетную запись TP-Link в облаке).

4. familiar-ai автоматически запускает go2rtc при запуске. Если ваша камера поддерживает двусторонний звук (обратная связь), голос воспроизводится из динамика камеры.

#### B) Локальный динамик ПК

По умолчанию (`TTS_OUTPUT=local`). Пытается воспроизводить с помощью: **paplay** → **mpv** → **ffplay**. Также используется в качестве резервного варианта, когда `TTS_OUTPUT=remote` и go2rtc недоступен.

| ОС | Установка |
|----|----------|
| macOS | `brew install mpv` |
| Ubuntu / Debian | `sudo apt install mpv` (или `paplay` через `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — установите `PULSE_SERVER=unix:/mnt/wslg/PulseServer` в `.env` |
| Windows | [mpv.io/installation](https://mpv.io/installation/) — скачайте и добавьте в PATH, **или** `winget install ffmpeg` |

> Если ни один аудиоплеер недоступен, речь все равно генерируется — но она не будет воспроизводиться.

### Ввод голоса (Realtime STT)

Установите `REALTIME_STT=true` в `.env` для голосового ввода без рук:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # тот же ключ, что и для TTS
```

familiar-ai передает аудио с микрофона ElevenLabs Scribe v2 и автоматически фиксирует транскрипции, когда вы делаете паузу в разговоре. Никакой кнопки нажимать не требуется. Сосуществует с режимом нажатия кнопки для разговора (Ctrl+T).

---

## TUI

familiar-ai включает терминальный интерфейс, построенный на [Textual](https://textual.textualize.io/):

- Прокручиваемая история разговоров с живым текстом
- Автозаполнение для `/quit`, `/clear`
- Прерывайте агента в середине его реплики, просто печатая, пока он думает
- **История разговоров** автоматически сохраняется в `~/.cache/familiar-ai/chat.log`

Чтобы следить за журналом в другом терминале (полезно для копирования-вставки):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## Личность (ME.md)

Личность вашего знакомого хранится в `ME.md`. Этот файл занесен в gitignore — он только ваш.

Посмотрите [`persona-template/en.md`](./persona-template/en.md) для примера или [`persona-template/ja.md`](./persona-template/ja.md) для японской версии.

---

## Часто задаваемые вопросы

**В: Работает ли он без GPU?**
Да. Модель эмбеддинга (multilingual-e5-small) работает хорошо на CPU. GPU ускоряет работу, но не является обязательным.

**В: Могу ли я использовать камеру, отличную от Tapo?**
Любая камера, поддерживающая ONVIF + RTSP, должна работать. Tapo C220 — это то, с чем мы проводили тесты.

**В: Отправляются ли мои данные куда-либо?**
Изображения и текст отправляются на выбранный вами API LLM для обработки. Воспоминания хранятся локально в `~/.familiar_ai/`.

**В: Почему агент пишет `（...）`, а не говорит?**
Убедитесь, что `ELEVENLABS_API_KEY` установлен. Без него голос отключен и агент возвращается к текстовому ответу.

## Технический фон

Интересно, как это работает? Смотрите [docs/technical.md](./docs/technical.md) для исследования и дизайнерских решений, лежащих в основе familiar-ai — ReAct, SayCan, Reflexion, Voyager, система желаний и многое другое.

---

## Участие

familiar-ai — это открытый эксперимент. Если что-то из этого вам близко — технически или философски — ваши вклады очень приветствуются.

**Хорошие места для начала:**

| Область | Что нужно |
|---------|-----------|
| Новое оборудование | Поддержка большего количества камер (RTSP, IP Webcam), микрофонов, приводов |
| Новые инструменты | Веб-поиск, автоматизация дома, календарь, что угодно через MCP |
| Новые бэкенды | Любой LLM или локальная модель, которая подходит под интерфейс `stream_turn` |
| Шаблоны личности | Шаблоны ME.md для разных языков и личностей |
| Исследования | Лучшие модели желаний, извлечение памяти, подсказки теории разума |
| Документация | Учебники, инструкции, переводы |

Смотрите [CONTRIBUTING.md](./CONTRIBUTING.md) для настройки разработки, стиля кода и правил PR.

Если вы не уверены, с чего начать, [откройте issue](https://github.com/lifemate-ai/familiar-ai/issues) — будем рады направить вас в правильном направлении.

---

## Лицензия

[MIT](./LICENSE)
```
