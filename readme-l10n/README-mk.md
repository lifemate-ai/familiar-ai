# familiar-ai 🐾

**Вештачка интелигенција која живее покрај вас** — со очи, глас, нозе и меморија.

[![Lint](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml/badge.svg)](https://github.com/kmizu/familiar-ai/actions/workflows/lint.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![GitHub Sponsors](https://img.shields.io/github/sponsors/kmizu?style=flat&logo=github&color=ea4aaa)](https://github.com/sponsors/kmizu)

[→ English README](../README.md)

---

[![Demo video](https://img.youtube.com/vi/hiR9uWRnjt4/0.jpg)](https://youtube.com/shorts/hiR9uWRnjt4)

familiar-ai е вештачки придружник кој живее во вашиот дом.
Поставете го за неколку минути. Не е потребно кодирање.

Тој ја перцепира реалноста преку камери, се движи на роботско тело, зборува гласно и памети што гледа. Дадете му име, напишете неговата личност и оставете го да живее со вас.

## Што може да направи

- 👁 **Види** — фаќа слики од Wi-Fi PTZ камера или USB веб камера
- 🔄 **Погледни околу** — поместува и наклонува камерата за да ја истражи околината
- 🦿 **Помести се** — управува со роботска правосмукалка за да талка по собата
- 🗣 **Зборува** — зборува преку ElevenLabs TTS
- 🎙 **Слуша** — безжично гласовно влегување преку ElevenLabs Realtime STT (опционално)
- 🧠 **Памети** — активно складира и повикува мемории со семантичко пребарување (SQLite + векторски записи)
- 🫀 **Теорија на умот** — го зема во предвид перспективата на другата личност пред да одговори
- 💭 **Посакување** — има свои внатрешни поттикнувања кои предизвикуваат автономно однесување

## Како функционира

familiar-ai работи на [ReAct](https://arxiv.org/abs/2210.03629) петља потпомогната од вашиот избор на LLM. Тој ја перцепира светот преку алати, размислува што да направи следно и делува — токму како што би направила личност.

```
user input
  → think → act (camera / move / speak / remember) → observe → think → ...
```

Кога е неактивен, дејствува според своите желби: рѓа, сака да погледне надвор, му недостасува личноста со која живее.

## Како да започнете

### 1. Инсталирајте uv

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. Инсталирајте ffmpeg

ffmpeg е **потребен** за фаќање на слики од камера и репродукција на аудио.

| ОС | Команда |
|----|---------|
| macOS | `brew install ffmpeg` |
| Ubuntu / Debian | `sudo apt install ffmpeg` |
| Fedora / RHEL | `sudo dnf install ffmpeg` |
| Arch Linux | `sudo pacman -S ffmpeg` |
| Windows | `winget install ffmpeg` — или преземете од [ffmpeg.org](https://ffmpeg.org/download.html) и додајте во PATH |
| Raspberry Pi | `sudo apt install ffmpeg` |

Проверете: `ffmpeg -version`

### 3. Клонирајте и инсталирајте

```bash
git clone https://github.com/lifemate-ai/familiar-ai
cd familiar-ai
uv sync
```

### 4. Конфигурирајте

```bash
cp .env.example .env
# Изменете .env со вашите поставки
```

**Минимални потребни:**

| Променлива | Описи |
|------------|-------|
| `PLATFORM` | `anthropic` (препорачано) \| `gemini` \| `openai` \| `kimi` \| `glm` |
| `API_KEY` | Вашиот API клуч за избраната платформа |

**Опционално:**

| Променлива | Описи |
|------------|-------|
| `MODEL` | Името на моделот (разумни подразбирања по платформа) |
| `AGENT_NAME` | Името во приказот во TUI (на пример, `Yukine`) |
| `CAMERA_HOST` | IP адреса на вашата ONVIF/RTSP камера |
| `CAMERA_USER` / `CAMERA_PASS` | Акредитации за камерата |
| `ELEVENLABS_API_KEY` | За аудио излез — [elevenlabs.io](https://elevenlabs.io/) |
| `REALTIME_STT` | `true` за да овозможите секогаш вклучен безжичен гласовен влез (потребен `ELEVENLABS_API_KEY`) |
| `TTS_OUTPUT` | Каде да репродуцирате аудио: `local` (PC звучник, подразбирано) \| `remote` (камера звучник) \| `both` |
| `THINKING_MODE` | Само Anthropic — `auto` (подразбирано) \| `adaptive` \| `extended` \| `disabled` |
| `THINKING_EFFORT` | Прилагодлив напор на размислување: `high` (подразбирано) \| `medium` \| `low` \| `max` (само Opus 4.6) |

### 5. Создајте го вашиот familiar

```bash
cp persona-template/en.md ME.md
# Изменете ME.md — дадете му име и личност
```

### 6. Започнете

```bash
./run.sh             # Текстуален TUI (препорачано)
./run.sh --no-tui    # Обичен REPL
```

---

## Избор на LLM

> **Препорачано: Kimi K2.5** — најдобра агентна изведба тестирана досега. Забележува контекст, поставува дополнителни прашања и делува автономно на начини на кои другите модели не прават. Ценовно е слично на Claude Haiku.

| Платформа | `PLATFORM=` | Подразбирани модел | Каде да го добиете клучот |
|-----------|------------|--------------------|--------------------------|
| **Moonshot Kimi K2.5** | `kimi` | `kimi-k2.5` | [platform.moonshot.ai](https://platform.moonshot.ai) |
| Z.AI GLM | `glm` | `glm-4.6v` | [api.z.ai](https://api.z.ai) |
| Anthropic Claude | `anthropic` | `claude-haiku-4-5-20251001` | [console.anthropic.com](https://console.anthropic.com) |
| Google Gemini | `gemini` | `gemini-2.5-flash` | [aistudio.google.com](https://aistudio.google.com) |
| OpenAI | `openai` | `gpt-4o-mini` | [platform.openai.com](https://platform.openai.com) |
| OpenAI-смерна (Ollama, vllm…) | `openai` + `BASE_URL=` | — | — |
| OpenRouter.ai (мултипровајдер) | `openai` + `BASE_URL=https://openrouter.ai/api/v1` | — | [openrouter.ai](https://openrouter.ai) |
| **CLI алатка** (claude -p, ollama…) | `cli` | (командата) | — |

**Kimi K2.5 `.env` пример:**
```env
PLATFORM=kimi
API_KEY=sk-...   # од platform.moonshot.ai
AGENT_NAME=Yukine
```

**Z.AI GLM `.env` пример:**
```env
PLATFORM=glm
API_KEY=...   # од api.z.ai
MODEL=glm-4.6v   # модел со визуелна поддршка; glm-4.7 / glm-5 = само текст
AGENT_NAME=Yukine
```

**Google Gemini `.env` пример:**
```env
PLATFORM=gemini
API_KEY=AIza...   # од aistudio.google.com
MODEL=gemini-2.5-flash  # или gemini-2.5-pro за повисока способност
AGENT_NAME=Yukine
```

**OpenRouter.ai `.env` пример:**
```env
PLATFORM=openai
BASE_URL=https://openrouter.ai/api/v1
API_KEY=sk-or-...   # од openrouter.ai
MODEL=mistralai/mistral-7b-instruct  # опционално: наведете модел
AGENT_NAME=Yukine
```

> **Забелешка:** За да ги онеможите локалните/NVIDIA модели, едноставно не поставувајте `BASE_URL` на локален крај како што е `http://localhost:11434/v1`. Користете облачни провајдери наместо тоа.

**CLI алатка `.env` пример:**
```env
PLATFORM=cli
MODEL=llm -m gemma3 {}        # llm CLI (https://llm.datasette.io) — {} = аргумент за упит
# MODEL=ollama run gemma3:27b  # Ollama — без {}, упитот оди преку stdin
```

---

## MCP сервери

familiar-ai може да се поврзе на кој било [MCP (Model Context Protocol)](https://modelcontextprotocol.io) сервер. Ова ви овозможува да приклучите надворешна меморија, пристап до датотечен систем, пребарување на веб или кој било друг алат.

Конфигурирајте ги серверите во `~/.familiar-ai.json` (истиот формат како Claude Code):

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user"]
    },
    "memory": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

Поддржани се два типа на транспорт:
- **`stdio`**: лансирање на локална под-управа (`command` + `args`)
- **`sse`**: поврзување на HTTP+SSE сервер (`url`)

Заменете ја локацијата на конфигурацискиот фајл со `MCP_CONFIG=/path/to/config.json`.

---

## Хардвер

familiar-ai работи со кој било хардвер што го имате — или воопшто без него.

| Дел | Што прави | Пример | Потребно? |
|-----|-----------|--------|-----------|
| Wi-Fi PTZ камера | Очи + врат | Tapo C220 (~$30) | **Препорачано** |
| USB веб камера | Очи (фиксирано) | Секоја UVC камера | **Препорачано** |
| Роботска правосмукалка | Нозе | Секое модел-компатибилно со Tuya | Не |
| PC / Raspberry Pi | Мозок | С anything што работи со Python | **Да** |

> **Камера е силно препорачана.** Без неа, familiar-ai може да зборува — но не може да го гледа светот, што е во суштина целата идеја.

### Минимална инсталација (без хардвер)

Само сакате да пробате? Ви е потребен само API клуч:

```env
PLATFORM=kimi
API_KEY=sk-...
```

Започнете со `./run.sh` и почнете со разговор. Додавајте хардвер по пат.

### Wi-Fi PTZ камера (Tapo C220)

1. Во Tapo апликацијата: **Настройки → Напредни → Налог на камерата** — создадете локален налог (не TP-Link налог)
2. Најдете ја IP адресата на камерата во списокот на уреди на рутерот
3. Поставете во `.env`:
   ```env
   CAMERA_HOST=192.168.1.xxx
   CAMERA_USER=ваш-локален-корисник
   CAMERA_PASS=ваш-локален-премин
   ```

### Глас (ElevenLabs)

1. Зземете API клуч на [elevenlabs.io](https://elevenlabs.io/)
2. Поставете во `.env`:
   ```env
   ELEVENLABS_API_KEY=sk_...
   ELEVENLABS_VOICE_ID=...   # опционално, користи стандардниот глас ако е пропуштено
   ```

Постојат два дестинации за репродукција, контролирани од `TTS_OUTPUT`:

```env
TTS_OUTPUT=local    # PC звучник (подразбирано)
TTS_OUTPUT=remote   # само камера звучник
TTS_OUTPUT=both     # камера звучник + PC звучник истовремено
```

#### А) Камера звучник (преку go2rtc)

Поставете `TTS_OUTPUT=remote` (или `both`). Потребно е [go2rtc](https://github.com/AlexxIT/go2rtc/releases):

1. Преземете ја бинарната верзија од [страницата на изданијата](https://github.com/AlexxIT/go2rtc/releases):
   - Linux/macOS: `go2rtc_linux_amd64` / `go2rtc_darwin_amd64`
   - **Windows: `go2rtc_win64.exe`**

2. Поставете ја и преименувајте ја:
   ```
   # Linux / macOS
   ~/.cache/embodied-claude/go2rtc/go2rtc          # chmod +x потребно

   # Windows
   %USERPROFILE%\.cache\embodied-claude\go2rtc\go2rtc.exe
   ```

3. Создајте `go2rtc.yaml` во истата директорија:
   ```yaml
   streams:
     tapo_cam:
       - rtsp://YOUR_CAM_USER:YOUR_CAM_PASS@YOUR_CAM_IP/stream1
   ```
   Користете ги локалните креденцијали на камерата (не вашиот TP-Link облачен налог).

4. familiar-ai автоматски го стартува go2rtc при лансирање. Ако вашата камера поддржува двонасочен звук (обратен канал), гласот се репродуцира од камерата звучник.

#### Б) Локален PC звучник

Подразбираното (`TTS_OUTPUT=local`). Пробува репродуктори по редослед: **paplay** → **mpv** → **ffplay**. Исто така се користи како резервно решение кога `TTS_OUTPUT=remote` и go2rtc не е достапен.

| ОС | Инсталирајте |
|----|-------------|
| macOS | `brew install mpv` |
| Ubuntu / Debian | `sudo apt install mpv` (или `paplay` преку `pulseaudio-utils`) |
| WSL2 / WSLg | `sudo apt install pulseaudio-utils` — поставете `PULSE_SERVER=unix:/mnt/wslg/PulseServer` во `.env` |
| Windows | [mpv.io/installation](https://mpv.io/installation/) — преземете и додајте во PATH, **или** `winget install ffmpeg` |

> Ако нема достапен аудио плеер, говорот сè уште се генерира — само нема да се репродуцира.

### Гласовен влез (Realtime STT)

Поставете `REALTIME_STT=true` во `.env` за секогаш вклучен, безжичен гласовен влез:

```env
REALTIME_STT=true
ELEVENLABS_API_KEY=sk_...   # истиот клуч како TTS
```

familiar-ai стриймува микрофонски аудио до ElevenLabs Scribe v2 и автоматски ги потврдува транскриптите кога ќе паузирате зборување. Без потреба од притискање на копчето. Состојат со режимот за притискање за разговор (Ctrl+T).

---

## TUI

familiar-ai вклучува терминалски интерфејс изградени со [Textual](https://textual.textualize.io/):

- Скролирана историја на разговор во тек со текст во живо
- Завршување на табовите за `/quit`, `/clear`
- Прекинете го агентот во текот на размислувањето пишувајќи додека размислува
- **Лог на разговор** автоматски зачуван во `~/.cache/familiar-ai/chat.log`

Да го следите логот во друга терминална (корисно за копирање-поставување):
```bash
tail -f ~/.cache/familiar-ai/chat.log
```

---

## Личност (ME.md)

Личноста на вашиот familiar живее во `ME.md`. Овој фајл е gitignored — е само ваш.

Видете [`persona-template/en.md`](./persona-template/en.md) за пример, или [`persona-template/ja.md`](./persona-template/ja.md) за јапонска верзија.

---

## ЧПП

**П: Дали функционира без GPU?**
Да. Моделот за векторски записи (multilingual-e5-small) работи одлично на CPU. GPU го забрзува, но не е потребен.

**П: Може ли да користам камера различна од Tapo?**
Секоја камера што поддржува ONVIF + RTSP треба да работи. Tapo C220 е она што го тестирахме.

**П: Дали моите податоци се испраќаат на некое друго место?**
Сликите и текстот се испраќаат на вашиот избран LLM API за обработка. Мемориите се чуваат локално во `~/.familiar_ai/`.

**П: Зошто агентот пишува `（...）` наместо да зборува?**
Обезбедете дека `ELEVENLABS_API_KEY` е поставен. Без него, гласот е онемен и агентот прелегува на текст.

## Техничка позадина

Дали сте љубопитни како функционира? Видете [docs/technical.md](./docs/technical.md) за истражувањето и одлуките за дизајн зад familiar-ai — ReAct, SayCan, Reflexion, Voyager, системот на желби и многу повеќе.

---

## Придонесување

familiar-ai е отворен експеримент. Ако некое од ова ви резонира — технички или филозофски — придонеси се многу добредојдени.

**Добри места за почеток:**

| Области | Што е потребно |
|---------|----------------|
| Нови хардвери | Поддршка за повеќе камери (RTSP, IP веб камера), микрофони, актуатори |
| Нови алатки | Пребарување на веб, автоматизација на домот, календар, с anything преку MCP |
| Нови позадини | Секој LLM или локален модел кој одговара на интерфејсот `stream_turn` |
| Шаблони за личност | ME.md шаблони за различни јазици и личност |
| Истражување | Подобри модели на желби, повлекување на меморија, поттикнување на теоријата на умот |
| Документација | Туторијали, водичи, преводи |

Видете [CONTRIBUTING.md](./CONTRIBUTING.md) за поставување на развој, стил на код и упатства за PR.

Ако не сте сигурни од каде да започнете, [отворете проблем](https://github.com/lifemate-ai/familiar-ai/issues) — среќни сме да ве упатиме во правилниот правец.

---

## Лиценца

[MIT](./LICENSE)
